# P1 第二批实现计划：细纲契约 / 文风锚点 / 爽点循环完成率

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 P1 #7（章节卡细纲契约 + 同型钩子检查）、#9（文风锚点 few-shot）、#10（爽点循环完成率），spec 见 `docs/superpowers/specs/2026-06-13-p1-batch2-design.md`。

**Architecture:** 迁移 v13 一次覆盖 chapter_cards 加列 + style_anchors 新表（双路径：schema.sql 基线 + migrations 链）。钩子枚举宽容归一（`craft/hooks.py`），volume_plan 生产、chapter_goal/评委消费、craft_check 确定性检查同型钩子。锚点手动录入（API CRUD），orchestrator RECALL 后按 target_emotion 选段注入 draft/polish prompt 的动态段（不碰稳定前缀）。完成率在 pipeline/stats 现算，纯确定性证据，零 LLM 零落库。

**Tech Stack:** Python/FastAPI/SQLite + pytest（FakeProvider 工厂）；React/TS 前端最小集。

**分支：** `feat/p1-batch2`（从 main 切出）；测试统一 `.venv/bin/python -m pytest`，工作目录 `/Users/nasa1024/workspace/zi`。

---

### Task 0: 切分支

- [ ] **Step 1: 从 main 切出 feat/p1-batch2**

```bash
cd /Users/nasa1024/workspace/zi && git checkout -b feat/p1-batch2
```

---

### Task 1: Migration v13（chapter_cards 4 列 + style_anchors 表）

**Files:**
- Create: `novelforge/db/migrations/v13.py`
- Modify: `novelforge/db/migrations/__init__.py`（import 链尾部加 v13）
- Modify: `novelforge/__init__.py`（SCHEMA_VERSION = "13"）
- Modify: `novelforge/db/schema.sql`（chapter_cards 加 4 列 + style_anchors 表）
- Test: `tests/test_migrations.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_migrations.py` 追加（仿照 v12 测试的结构；老库迁移测试 `test_migrate_from_v4` 中追加 v13 断言）：

```python
def test_v13_fresh_db_has_contract_columns_and_style_anchors(self):
    """新库基线（schema.sql）直接含 v13 形态。"""
    import sqlite3
    from novelforge.db.connection import init_db_from_conn
    conn = sqlite3.connect(":memory:")
    conn.row_factory = sqlite3.Row
    init_db_from_conn(conn)
    cols = {r["name"] for r in conn.execute("PRAGMA table_info(chapter_cards)")}
    assert {"target_emotion", "opening_hook_type", "hook_type", "expectation_score"} <= cols
    anchor_cols = {r["name"] for r in conn.execute("PRAGMA table_info(style_anchors)")}
    assert {"id", "emotion", "title", "content", "enabled", "created_at"} <= anchor_cols
    conn.close()

def test_v13_alters_existing_chapter_cards(self):
    """老库（v12 形态 chapter_cards）迁移后补列且数据保留。"""
    import sqlite3
    from novelforge.db.migrations import migrate
    conn = sqlite3.connect(":memory:")
    conn.row_factory = sqlite3.Row
    conn.executescript("""
        CREATE TABLE meta_kv (key TEXT PRIMARY KEY, value TEXT NOT NULL,
                              updated_at TEXT NOT NULL DEFAULT (datetime('now')));
        INSERT INTO meta_kv(key, value) VALUES('schema_version', '12');
        CREATE TABLE chapter_cards (
            id TEXT PRIMARY KEY, chapter INTEGER NOT NULL UNIQUE,
            title TEXT, pov_entity_id TEXT, goal TEXT, summary TEXT,
            word_count INTEGER, hook_text TEXT,
            status TEXT NOT NULL DEFAULT 'planned', draft_id TEXT,
            created_at TEXT NOT NULL DEFAULT (datetime('now')));
        INSERT INTO chapter_cards(id, chapter, title) VALUES('c1', 1, '旧章');
    """)
    migrate(conn)
    row = conn.execute("SELECT * FROM chapter_cards WHERE chapter=1").fetchone()
    assert row["title"] == "旧章"
    assert row["hook_type"] is None
    assert conn.execute("SELECT 1 FROM style_anchors LIMIT 0") is not None
    conn.close()
```

并在 `test_migrate_from_v4`（老库全链路迁移测试）的断言区追加：

```python
        # v13: 细纲契约列 + style_anchors
        cols = {r["name"] for r in conn.execute("PRAGMA table_info(chapter_cards)")}
        assert {"target_emotion", "opening_hook_type", "hook_type", "expectation_score"} <= cols
        assert _table_exists(conn, "style_anchors")
```

（若该测试文件无 `_table_exists` 辅助，用 `conn.execute("SELECT 1 FROM style_anchors LIMIT 0")` 代替。注意：v4 老库**没有 chapter_cards 表**——和 foreshadow 同一个坑，v13 必须处理缺表路径。）

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_migrations.py -x -q
```
预期：FAIL（style_anchors 不存在 / 列缺失 / KeyError）。

- [ ] **Step 3: 写 v13 迁移**

`novelforge/db/migrations/v13.py`：

```python
"""Migration v12 → v13: 章节卡细纲契约列 + style_anchors 表（P1#7/#9）。

chapter_cards 历史上同样只在 schema.sql 基线里（无迁移创建过），
v4 老库可能整表缺失——缺表按 v13 形态整建，存在则补列。
"""
from __future__ import annotations
import sqlite3
from . import register, column_exists, table_exists

_CHAPTER_CARDS_V13 = """
CREATE TABLE IF NOT EXISTS chapter_cards (
    id              TEXT PRIMARY KEY,
    chapter         INTEGER NOT NULL UNIQUE,
    title           TEXT,
    pov_entity_id   TEXT,
    goal            TEXT,
    summary         TEXT,
    word_count      INTEGER,
    hook_text       TEXT,
    status          TEXT NOT NULL DEFAULT 'planned'
                        CHECK(status IN ('planned','drafted','reviewed','committed')),
    draft_id        TEXT,
    target_emotion    TEXT,
    opening_hook_type TEXT,
    hook_type         TEXT,
    expectation_score INTEGER,
    created_at      TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY(pov_entity_id) REFERENCES entities(id)
);
"""

_STYLE_ANCHORS = """
CREATE TABLE IF NOT EXISTS style_anchors (
    id          TEXT PRIMARY KEY,
    emotion     TEXT NOT NULL,
    title       TEXT,
    content     TEXT NOT NULL,
    enabled     INTEGER NOT NULL DEFAULT 1,
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_style_anchors_emotion ON style_anchors(emotion);
"""


@register("13")
def migrate_v13(conn: sqlite3.Connection) -> None:
    if not table_exists(conn, "chapter_cards"):
        conn.executescript(_CHAPTER_CARDS_V13)
    else:
        for col, decl in (
            ("target_emotion", "TEXT"),
            ("opening_hook_type", "TEXT"),
            ("hook_type", "TEXT"),
            ("expectation_score", "INTEGER"),
        ):
            if not column_exists(conn, "chapter_cards", col):
                conn.execute(f"ALTER TABLE chapter_cards ADD COLUMN {col} {decl}")
    conn.executescript(_STYLE_ANCHORS)
```

`novelforge/db/migrations/__init__.py` 的 import 链尾部加 `from . import v13  # noqa: E402,F401`（仿 v12 行）。

`novelforge/__init__.py`：`SCHEMA_VERSION = "13"  # +细纲契约列 + style_anchors`。

`novelforge/db/schema.sql`：chapter_cards 表 `draft_id` 行后加：

```sql
    target_emotion    TEXT,                        -- v13: 本章目标情绪词
    opening_hook_type TEXT,                        -- v13: 章首钩子（7 式或 other）
    hook_type         TEXT,                        -- v13: 章尾钩子（13 式或 other）
    expectation_score INTEGER,                     -- v13: 期待度 1-5
```

并在 chapter_cards 表定义之后加 style_anchors（含 v13 注释）：

```sql
-- v13: 文风锚点（P1#9——手动录入范文段，按情绪做 few-shot）
CREATE TABLE IF NOT EXISTS style_anchors (
    id          TEXT PRIMARY KEY,
    emotion     TEXT NOT NULL,
    title       TEXT,
    content     TEXT NOT NULL,
    enabled     INTEGER NOT NULL DEFAULT 1,
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_style_anchors_emotion ON style_anchors(emotion);
```

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_migrations.py -q
```
预期：全 PASS。

- [ ] **Step 5: 提交**

```bash
git add novelforge/db/migrations/v13.py novelforge/db/migrations/__init__.py novelforge/__init__.py novelforge/db/schema.sql tests/test_migrations.py
git commit -m "feat(P1#7/#9): migration v13——chapter_cards 细纲契约列 + style_anchors 表"
```

---

### Task 2: 钩子枚举与归一（craft/hooks.py）

**Files:**
- Create: `novelforge/craft/hooks.py`
- Test: `tests/test_p1_batch2.py`（新文件）

- [ ] **Step 1: 写失败测试**

`tests/test_p1_batch2.py` 新建：

```python
"""P1 第二批测试：细纲契约（#7）/ 文风锚点（#9）/ 爽点循环完成率（#10）。

spec: docs/superpowers/specs/2026-06-13-p1-batch2-design.md
"""
from __future__ import annotations

import json
import sqlite3

import pytest


@pytest.fixture
def conn():
    from novelforge.db.connection import init_db_from_conn
    c = sqlite3.connect(":memory:")
    c.row_factory = sqlite3.Row
    init_db_from_conn(c)
    yield c
    c.close()


class TestHookNormalize:
    def test_exact_enum_key_passthrough(self):
        from novelforge.craft.hooks import normalize_hook_type
        assert normalize_hook_type("reversal", "ending") == "reversal"
        assert normalize_hook_type("suspense", "opening") == "suspense"

    def test_chinese_keyword_maps_to_enum(self):
        from novelforge.craft.hooks import normalize_hook_type
        assert normalize_hook_type("反转式", "ending") == "reversal"
        assert normalize_hook_type("命悬一线", "ending") == "cliffhanger"
        assert normalize_hook_type("悬念开局", "opening") == "suspense"

    def test_unrecognizable_returns_other(self):
        from novelforge.craft.hooks import normalize_hook_type
        assert normalize_hook_type("写得很好看", "ending") == "other"
        assert normalize_hook_type("", "ending") == "other"
        assert normalize_hook_type(None, "opening") == "other"

    def test_wrong_kind_enum_not_passthrough(self):
        from novelforge.craft.hooks import normalize_hook_type
        # cliffhanger 是章尾式，不在章首 7 式里
        assert normalize_hook_type("cliffhanger", "opening") == "other"

    def test_hook_label_renders_chinese(self):
        from novelforge.craft.hooks import hook_label
        assert hook_label("reversal") == "反转"
        assert hook_label("other") == "other"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestHookNormalize -x -q
```
预期：FAIL（ModuleNotFoundError: novelforge.craft.hooks）。

- [ ] **Step 3: 实现 hooks.py**

`novelforge/craft/hooks.py`：

```python
"""钩子类型枚举与宽容归一（P1#7，oh-story 章首 7 式 / 章尾 13 式）。

枚举 key 用英文存库；LLM/人工输入经 normalize_hook_type 归一，
匹配不上存 'other'——other 不参与「连续两章同型钩子」检查。
存库不加 CHECK 约束：归一函数是唯一入口，约束放代码层。
"""
from __future__ import annotations

from typing import Optional

# key → (中文名, 关键词表)。关键词按"包含"匹配，先长后短由 dict 序保证。
OPENING_HOOK_TYPES: dict[str, tuple[str, list[str]]] = {
    "suspense":  ("悬念", ["悬念", "谜团开局", "疑问"]),
    "conflict":  ("冲突", ["冲突", "对峙", "争执"]),
    "dialogue":  ("对话切入", ["对话切入", "对话开场", "对白"]),
    "action":    ("动作", ["动作", "打斗", "战斗开场", "追逐"]),
    "anomaly":   ("反常", ["反常", "异常", "诡异"]),
    "crisis":    ("危机", ["危机", "命悬", "险境", "绝境"]),
    "flashback": ("倒叙", ["倒叙", "回忆切入", "闪回"]),
}

ENDING_HOOK_TYPES: dict[str, tuple[str, list[str]]] = {
    "cliffhanger":  ("命悬一线", ["命悬一线", "命悬", "千钧一发", "生死关头"]),
    "reversal":     ("反转", ["反转", "逆转", "翻转"]),
    "reveal":       ("揭秘", ["揭秘", "真相", "揭露", "身份曝光"]),
    "new_threat":   ("新威胁", ["新威胁", "强敌", "大敌", "威胁逼近"]),
    "mystery":      ("新谜团", ["新谜团", "谜团", "疑团", "未知"]),
    "promise":      ("承诺约战", ["约战", "承诺", "赌约", "之约"]),
    "arrival":      ("神秘登场", ["神秘人", "登场", "现身", "驾到"]),
    "decision":     ("重大抉择", ["抉择", "选择", "两难"]),
    "countdown":    ("倒计时", ["倒计时", "期限", "时限", "大限"]),
    "loss":         ("失去代价", ["失去", "代价", "牺牲", "陨落"]),
    "power_tease":  ("力量预告", ["力量预告", "觉醒前兆", "突破在即", "异变"]),
    "relationship": ("关系变化", ["关系", "决裂", "结盟", "告白"]),
    "humiliation":  ("受辱蓄势", ["受辱", "羞辱", "蓄势", "隐忍"]),
}


def _enum_for(kind: str) -> dict[str, tuple[str, list[str]]]:
    return OPENING_HOOK_TYPES if kind == "opening" else ENDING_HOOK_TYPES


def normalize_hook_type(raw: Optional[str], kind: str) -> str:
    """归一钩子类型：精确 key → 中文名/关键词包含 → 'other'。kind: opening|ending。"""
    if not raw or not isinstance(raw, str):
        return "other"
    enum = _enum_for(kind)
    text = raw.strip().lower()
    if text in enum:
        return text
    for key, (cn, keywords) in enum.items():
        if cn in raw or any(k in raw for k in keywords):
            return key
    return "other"


def hook_label(key: Optional[str]) -> str:
    """枚举 key → 中文名（用于 prompt/前端展示）；other/未知原样返回。"""
    if not key:
        return "other"
    for enum in (OPENING_HOOK_TYPES, ENDING_HOOK_TYPES):
        if key in enum:
            return enum[key][0]
    return key


def hook_menu(kind: str) -> str:
    """给 prompt 用的枚举菜单：'suspense(悬念)/conflict(冲突)/…'。"""
    enum = _enum_for(kind)
    return "/".join(f"{k}({v[0]})" for k, v in enum.items())
```

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestHookNormalize -q
```
预期：5 PASS。

- [ ] **Step 5: 提交**

```bash
git add novelforge/craft/hooks.py tests/test_p1_batch2.py
git commit -m "feat(P1#7): 钩子 7+13 式枚举 + 宽容归一（craft/hooks.py）"
```

---

### Task 3: volume_plan 生产侧——输出契约扩展

**Files:**
- Modify: `novelforge/skills/volume_plan_skill.py`（_SYSTEM prompt + _parse_plans）
- Modify: `novelforge/app/api/volumes.py`（INSERT 带新列 + _load_chapter_card）
- Modify: `novelforge/app/models.py`（ChapterCardModel / ChapterCardUpdateRequest 加字段）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

`tests/test_p1_batch2.py` 追加：

```python
class TestVolumePlanContract:
    def test_system_prompt_lists_hook_enums(self):
        from novelforge.skills.volume_plan_skill import _SYSTEM
        for marker in ("target_emotion", "opening_hook_type", "hook_type",
                       "expectation_score", "悬念", "反转", "相邻两章"):
            assert marker in _SYSTEM, f"prompt 缺契约标记: {marker}"

    def test_parse_normalizes_and_clamps(self):
        from novelforge.skills.volume_plan_skill import _parse_plans
        raw = json.dumps([{
            "chapter": 3, "title": "t", "goal": "g", "hook_text": "h",
            "target_emotion": "紧张",
            "opening_hook_type": "危机开局",
            "hook_type": "大反转",
            "expectation_score": 9,
            "beats": [{"beat_type": "hook", "summary": "s"}],
        }], ensure_ascii=False)
        plans = _parse_plans(f"```plans\n{raw}\n```", 3, 3)
        assert plans[0]["opening_hook_type"] == "crisis"
        assert plans[0]["hook_type"] == "reversal"
        assert plans[0]["expectation_score"] == 5      # clamp 到 1-5
        assert plans[0]["target_emotion"] == "紧张"

    def test_parse_missing_fields_tolerant(self):
        from novelforge.skills.volume_plan_skill import _parse_plans
        raw = json.dumps([{"chapter": 3, "title": "t", "goal": "g",
                           "hook_text": "h", "beats": []}], ensure_ascii=False)
        plans = _parse_plans(f"```plans\n{raw}\n```", 3, 3)
        assert plans[0]["hook_type"] == "other"
        assert plans[0]["expectation_score"] is None
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestVolumePlanContract -x -q
```
预期：FAIL（prompt 缺标记 / KeyError）。

- [ ] **Step 3: 实现**

`volume_plan_skill.py` 的 `_SYSTEM` 输出格式块中，每章 JSON 在 `"hook_text"` 行后加：

```
    "target_emotion": "本章目标情绪词（如 紧张/扬眉吐气/悲怆）",
    "opening_hook_type": "章首钩子，7 式选一: suspense(悬念)/conflict(冲突)/dialogue(对话切入)/action(动作)/anomaly(反常)/crisis(危机)/flashback(倒叙)",
    "hook_type": "章尾钩子，13 式选一: cliffhanger(命悬一线)/reversal(反转)/reveal(揭秘)/new_threat(新威胁)/mystery(新谜团)/promise(承诺约战)/arrival(神秘登场)/decision(重大抉择)/countdown(倒计时)/loss(失去代价)/power_tease(力量预告)/relationship(关系变化)/humiliation(受辱蓄势)",
    "expectation_score": 4,
```

规划规则区追加一条：

```
- 相邻两章 hook_type 不得相同（钩子同质化让读者疲劳）；expectation_score 是章尾钩子期待度 1-5
```

（菜单字符串可用 `hooks.hook_menu()` 生成后嵌入，但 prompt 是模块级常量——直接写死字符串更简单，枚举变更时两处同步；在 `_SYSTEM` 上方加注释指向 `craft/hooks.py`。）

`_parse_plans` 的 plans.append 块改为：

```python
        from ..craft.hooks import normalize_hook_type
        exp = p.get("expectation_score")
        try:
            exp = max(1, min(5, int(exp))) if exp is not None else None
        except (TypeError, ValueError):
            exp = None
        plans.append({
            "chapter": ch,
            "title": str(p.get("title", ""))[:100],
            "goal": str(p.get("goal", ""))[:1000],
            "hook_text": str(p.get("hook_text", ""))[:300],
            "target_emotion": (str(p.get("target_emotion"))[:50]
                               if p.get("target_emotion") else None),
            "opening_hook_type": normalize_hook_type(p.get("opening_hook_type"), "opening"),
            "hook_type": normalize_hook_type(p.get("hook_type"), "ending"),
            "expectation_score": exp,
            "beats": beats,
        })
```

（import 放函数体内顶部一次，不放循环里。）

`app/models.py`：`ChapterCardModel` 加：

```python
    target_emotion: Optional[str] = None
    opening_hook_type: Optional[str] = None
    hook_type: Optional[str] = None
    expectation_score: Optional[int] = None
```

`ChapterCardUpdateRequest` 加同样 4 个字段（PATCH 人审可改）。

`app/api/volumes.py`：
- `_load_chapter_card` 的 SELECT 加 4 列并传给 ChapterCardModel。
- `plan_volume` 落库 INSERT 改为：

```python
            conn.execute(
                "INSERT INTO chapter_cards(id, chapter, title, goal, hook_text,"
                " target_emotion, opening_hook_type, hook_type, expectation_score, status)"
                " VALUES(?,?,?,?,?,?,?,?,?,'planned')"
                " ON CONFLICT(chapter) DO UPDATE SET"
                "   title=excluded.title, goal=excluded.goal,"
                "   hook_text=excluded.hook_text,"
                "   target_emotion=excluded.target_emotion,"
                "   opening_hook_type=excluded.opening_hook_type,"
                "   hook_type=excluded.hook_type,"
                "   expectation_score=excluded.expectation_score,"
                "   status='planned'",
                (new_id("card"), ch, p.get("title"), p.get("goal"), p.get("hook_text"),
                 p.get("target_emotion"), p.get("opening_hook_type"),
                 p.get("hook_type"), p.get("expectation_score")),
            )
```

PATCH 端点用的是 `req.model_dump()` 动态 SET，加字段后自动生效，无需改。

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestVolumePlanContract tests/test_m4.py -q
```
预期：PASS（test_m4 是 volume_plan 既有测试，确认未破坏）。

- [ ] **Step 5: 提交**

```bash
git add novelforge/skills/volume_plan_skill.py novelforge/app/api/volumes.py novelforge/app/models.py tests/test_p1_batch2.py
git commit -m "feat(P1#7): volume_plan 输出细纲契约——目标情绪/钩子双枚举/期待度"
```

---

### Task 4: 消费侧——chapter_goal 注入 + 评委 ground truth

**Files:**
- Modify: `novelforge/app/chapter_suggest.py`（assemble_chapter_goal）
- Modify: `novelforge/craft/candidate_judge.py`（_JUDGE_SYSTEM / _SCORE_SYSTEM 措辞）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

```python
class TestContractConsumption:
    def test_chapter_goal_includes_contract_line(self, conn):
        from novelforge.app.chapter_suggest import assemble_chapter_goal
        conn.execute(
            "INSERT INTO chapter_cards(id, chapter, title, goal, target_emotion,"
            " opening_hook_type, hook_type, expectation_score)"
            " VALUES('c1', 5, '风起', '主角入城', '紧张', 'crisis', 'reversal', 4)")
        conn.commit()
        goal, sources = assemble_chapter_goal(conn, 5)
        assert "细纲契约" in goal
        assert "紧张" in goal and "危机" in goal and "反转" in goal and "4/5" in goal
        assert "chapter_card_contract" in sources

    def test_chapter_goal_no_contract_when_fields_empty(self, conn):
        from novelforge.app.chapter_suggest import assemble_chapter_goal
        conn.execute(
            "INSERT INTO chapter_cards(id, chapter, title, goal) VALUES('c1', 5, 't', 'g')")
        conn.commit()
        goal, sources = assemble_chapter_goal(conn, 5)
        assert "细纲契约" not in goal
        assert "chapter_card_contract" not in sources

    def test_judge_prompts_mention_contract(self):
        from novelforge.craft.candidate_judge import _JUDGE_SYSTEM, _SCORE_SYSTEM
        assert "承诺" in _JUDGE_SYSTEM
        assert "承诺" in _SCORE_SYSTEM
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestContractConsumption -x -q
```
预期：FAIL。

- [ ] **Step 3: 实现**

`chapter_suggest.py` 的 `assemble_chapter_goal`，章节卡查询改为带新列，卡片段之后追加契约行：

```python
    card = conn.execute(
        "SELECT title, goal, summary, target_emotion, opening_hook_type,"
        "       hook_type, expectation_score"
        " FROM chapter_cards WHERE chapter=?", (chapter,)
    ).fetchone()
    if card and (card["goal"] or card["summary"]):
        prefix = f"本章《{card['title']}》：" if card["title"] else "本章目标："
        goal_parts.append(prefix + (card["goal"] or card["summary"]))
        sources.append("chapter_card")

    # P1#7：细纲契约注入——目标情绪 + 钩子承诺，draft 照着写、评委照着验
    if card and (card["target_emotion"] or card["hook_type"]):
        from ..craft.hooks import hook_label
        parts = []
        if card["target_emotion"]:
            parts.append(f"目标情绪「{card['target_emotion']}」")
        if card["opening_hook_type"] and card["opening_hook_type"] != "other":
            parts.append(f"章首钩子={hook_label(card['opening_hook_type'])}式（开篇 100 字内）")
        if card["hook_type"] and card["hook_type"] != "other":
            exp = f"（期待度 {card['expectation_score']}/5）" if card["expectation_score"] else ""
            parts.append(f"章尾钩子={hook_label(card['hook_type'])}式{exp}")
        if parts:
            goal_parts.append("本章细纲契约：" + "；".join(parts))
            sources.append("chapter_card_contract")
```

注意 chapter_suggest.py 目前是顶层模块（`novelforge/app/`），import 用 `from ..craft.hooks import hook_label`。

`candidate_judge.py`：
- `_JUDGE_SYSTEM` 第①条改为：`① 钩子力度（章末悬念是否抓人；若本章目标中含细纲契约承诺的钩子类型，以是否兑现承诺为准）`
- `_SCORE_SYSTEM` hook 维度行改为：`hook=钩子力度（章末悬念是否抓人；本章目标含钩子承诺时，验承诺是否兑现）`

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestContractConsumption tests/test_m5.py tests/test_m3.py -q
```
预期：PASS（m3/m5 覆盖评委与建议链路回归）。

- [ ] **Step 5: 提交**

```bash
git add novelforge/app/chapter_suggest.py novelforge/craft/candidate_judge.py tests/test_p1_batch2.py
git commit -m "feat(P1#7): 细纲契约消费——chapter_goal 注入承诺 + 评委验兑现"
```

---

### Task 5: 确定性检查——连续两章同型钩子

**Files:**
- Modify: `novelforge/skills/craft_check_skill.py`（新增第 ⑧ 项 hook_repeat）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

```python
def _seed_cards(conn, *rows):
    """rows: (chapter, hook_type)"""
    for i, (ch, ht) in enumerate(rows):
        conn.execute(
            "INSERT INTO chapter_cards(id, chapter, hook_type) VALUES(?,?,?)",
            (f"card{i}", ch, ht))
    conn.commit()


class TestHookRepeatCheck:
    def _run(self, conn, chapter):
        from novelforge.skills.craft_check_skill import _check_hook_repeat
        return _check_hook_repeat(conn, chapter)

    def test_same_type_adjacent_warns(self, conn):
        _seed_cards(conn, (4, "reversal"), (5, "reversal"))
        issues = self._run(conn, 5)
        assert len(issues) == 1
        assert issues[0].severity == "warn"
        assert issues[0].check == "hook_repeat"

    def test_different_type_no_warn(self, conn):
        _seed_cards(conn, (4, "reversal"), (5, "cliffhanger"))
        assert self._run(conn, 5) == []

    def test_other_or_missing_skipped(self, conn):
        _seed_cards(conn, (4, "other"), (5, "other"))
        assert self._run(conn, 5) == []
        assert self._run(conn, 99) == []   # 无卡

    def test_null_hook_type_skipped(self, conn):
        _seed_cards(conn, (4, None), (5, "reversal"))
        assert self._run(conn, 5) == []
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestHookRepeatCheck -x -q
```
预期：FAIL（ImportError: _check_hook_repeat）。

- [ ] **Step 3: 实现**

`craft_check_skill.py` 确定性检查区追加：

```python
def _check_hook_repeat(conn, chapter: int) -> list[CraftIssue]:
    """P1#7：连续两章同型章尾钩子（oh-story 硬规则的 warn 化）。

    本章与上一章 hook_type 均为有效枚举（非 NULL/other）且相同 → warn。
    规划数据不全时静默跳过，不误报。
    """
    if conn is None or chapter < 2:
        return []
    try:
        rows = conn.execute(
            "SELECT chapter, hook_type FROM chapter_cards"
            " WHERE chapter IN (?, ?)", (chapter - 1, chapter),
        ).fetchall()
    except Exception:
        return []   # 老库无该列 → 跳过
    by_ch = {r["chapter"]: r["hook_type"] for r in rows}
    cur, prev = by_ch.get(chapter), by_ch.get(chapter - 1)
    if not cur or not prev or "other" in (cur, prev) or cur != prev:
        return []
    from ..craft.hooks import hook_label
    return [CraftIssue(
        check="hook_repeat", severity="warn",
        detail=f"连续两章使用同型章尾钩子（{hook_label(cur)}式，第{chapter-1}→{chapter}章），"
               f"建议本章换型防读者疲劳",
    )]
```

`run()` 中第⑦项之后调用：

```python
        # ⑧ hook_repeat：连续两章同型章尾钩子（P1#7，确定性零成本）
        issues += _check_hook_repeat(ctx.conn, ctx.target_chapter)
```

模块 docstring 的「7 项校验」更新为 8 项，加一行 `8. hook_repeat 连续两章同型钩子 warn`。

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestHookRepeatCheck tests/test_m2.py -q
```
预期：PASS。

- [ ] **Step 5: 提交**

```bash
git add novelforge/skills/craft_check_skill.py tests/test_p1_batch2.py
git commit -m "feat(P1#7): craft_check 第 8 项——连续两章同型钩子确定性检查"
```

---

### Task 6: style_anchors API CRUD

**Files:**
- Modify: `novelforge/app/models.py`（StyleAnchor 三模型）
- Modify: `novelforge/app/api/craft.py`（4 个端点）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

仿 `tests/test_p1_core.py::TestApiWiring` 的 TestClient 模式（用 `novelforge.app.main:create_app` + 临时项目；如该文件用了现成 fixture，照搬其写法）：

```python
class TestStyleAnchorApi:
    @pytest.fixture
    def client(self, tmp_path):
        from fastapi.testclient import TestClient
        from novelforge.app.main import create_app
        import novelforge.app.deps as deps
        app = create_app(data_dir=tmp_path)          # 以 test_p1_core 实际写法为准
        c = TestClient(app)
        c.post("/v1/projects", json={"name": "t"})
        pid = c.get("/v1/projects").json()[0]["id"]
        return c, pid

    def test_crud_roundtrip(self, client):
        c, pid = client
        r = c.post(f"/v1/{pid}/style-anchors", json={
            "emotion": "紧张", "title": "某书第3章", "content": "刀光一闪。" * 20})
        assert r.status_code == 201
        aid = r.json()["id"]
        r = c.get(f"/v1/{pid}/style-anchors", params={"emotion": "紧张"})
        assert len(r.json()) == 1
        r = c.patch(f"/v1/{pid}/style-anchors/{aid}", json={"enabled": False})
        assert r.json()["enabled"] is False
        assert c.delete(f"/v1/{pid}/style-anchors/{aid}").status_code == 204
        assert c.get(f"/v1/{pid}/style-anchors").json() == []

    def test_content_length_validated(self, client):
        c, pid = client
        r = c.post(f"/v1/{pid}/style-anchors", json={"emotion": "x", "content": "短"})
        assert r.status_code == 422
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestStyleAnchorApi -x -q
```
预期：FAIL（404 路由不存在）。

- [ ] **Step 3: 实现**

`models.py` foreshadow 模型附近追加：

```python
# ── Style anchors（P1#9 文风锚点 few-shot）────────────────────────────────────

class StyleAnchorCreate(BaseModel):
    emotion: str = Field(min_length=1, max_length=50)
    title: Optional[str] = Field(default=None, max_length=200)
    content: str = Field(min_length=50, max_length=2000)   # 300-500 字最佳，硬限 50-2000


class StyleAnchorUpdate(BaseModel):
    emotion: Optional[str] = Field(default=None, min_length=1, max_length=50)
    title: Optional[str] = Field(default=None, max_length=200)
    content: Optional[str] = Field(default=None, min_length=50, max_length=2000)
    enabled: Optional[bool] = None


class StyleAnchorResponse(BaseModel):
    id: str
    emotion: str
    title: Optional[str] = None
    content: str
    enabled: bool = True
    created_at: Optional[str] = None
```

`craft.py` 尾部追加 4 个端点（依赖注入仿 foreshadow 端点）：

```python
# ── Style anchors（P1#9）──────────────────────────────────────────────────────

def _row_to_anchor(row) -> StyleAnchorResponse:
    return StyleAnchorResponse(
        id=row["id"], emotion=row["emotion"], title=row["title"],
        content=row["content"], enabled=bool(row["enabled"]),
        created_at=row["created_at"],
    )


@router.post("/{project_id}/style-anchors", status_code=201)
def create_style_anchor(
    project_id: str, req: StyleAnchorCreate,
    registry: ProjectRegistry = Depends(get_registry),
) -> StyleAnchorResponse:
    conn = registry.open_conn(project_id)
    try:
        aid = new_id("anchor")
        conn.execute(
            "INSERT INTO style_anchors(id, emotion, title, content) VALUES(?,?,?,?)",
            (aid, req.emotion, req.title, req.content))
        conn.commit()
        row = conn.execute("SELECT * FROM style_anchors WHERE id=?", (aid,)).fetchone()
        return _row_to_anchor(row)
    finally:
        conn.close()


@router.get("/{project_id}/style-anchors")
def list_style_anchors(
    project_id: str, emotion: Optional[str] = None,
    registry: ProjectRegistry = Depends(get_registry),
) -> list[StyleAnchorResponse]:
    conn = registry.open_conn(project_id)
    try:
        if emotion:
            rows = conn.execute(
                "SELECT * FROM style_anchors WHERE emotion=? ORDER BY created_at DESC",
                (emotion,)).fetchall()
        else:
            rows = conn.execute(
                "SELECT * FROM style_anchors ORDER BY created_at DESC").fetchall()
        return [_row_to_anchor(r) for r in rows]
    finally:
        conn.close()


@router.patch("/{project_id}/style-anchors/{anchor_id}")
def update_style_anchor(
    project_id: str, anchor_id: str, req: StyleAnchorUpdate,
    registry: ProjectRegistry = Depends(get_registry),
) -> StyleAnchorResponse:
    conn = registry.open_conn(project_id)
    try:
        updates = {k: v for k, v in req.model_dump().items() if v is not None}
        if "enabled" in updates:
            updates["enabled"] = int(updates["enabled"])
        if updates:
            set_clause = ", ".join(f"{k}=?" for k in updates)
            cur = conn.execute(
                f"UPDATE style_anchors SET {set_clause} WHERE id=?",
                list(updates.values()) + [anchor_id])
            conn.commit()
            if cur.rowcount == 0:
                raise HTTPException(404, f"style_anchor 不存在: {anchor_id}")
        row = conn.execute(
            "SELECT * FROM style_anchors WHERE id=?", (anchor_id,)).fetchone()
        if row is None:
            raise HTTPException(404, f"style_anchor 不存在: {anchor_id}")
        return _row_to_anchor(row)
    finally:
        conn.close()


@router.delete("/{project_id}/style-anchors/{anchor_id}", status_code=204)
def delete_style_anchor(
    project_id: str, anchor_id: str,
    registry: ProjectRegistry = Depends(get_registry),
):
    conn = registry.open_conn(project_id)
    try:
        cur = conn.execute("DELETE FROM style_anchors WHERE id=?", (anchor_id,))
        conn.commit()
        if cur.rowcount == 0:
            raise HTTPException(404, f"style_anchor 不存在: {anchor_id}")
        return Response(status_code=204)
    finally:
        conn.close()
```

（StyleAnchor* 模型与 `new_id`、`Response` 的 import 补进 craft.py 头部；注意 PATCH 里 `enabled=False` 会被 `if v is not None` 保留——`model_dump()` 后 False 不是 None，但要确认 update dict 过滤逻辑用 `is not None` 而不是真值判断，上面代码已正确。）

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestStyleAnchorApi -q
```
预期：PASS。

- [ ] **Step 5: 提交**

```bash
git add novelforge/app/models.py novelforge/app/api/craft.py tests/test_p1_batch2.py
git commit -m "feat(P1#9): style_anchors API CRUD——文风锚点手动录入"
```

---

### Task 7: 锚点选取与注入（pick + orchestrator + draft/polish prompt）

**Files:**
- Create: `novelforge/craft/style_anchor.py`
- Modify: `novelforge/control_plane/orchestrator.py`（RECALL 段尾注入 workspace）
- Modify: `novelforge/skills/chapter_draft_skill.py`（user_msg 消费）
- Modify: `novelforge/control_plane/orchestrator.py::_polish`（润色 prompt 消费）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

```python
def _seed_anchor(conn, emotion, content, enabled=1):
    from novelforge.ids import new_id
    aid = new_id("anchor")
    conn.execute(
        "INSERT INTO style_anchors(id, emotion, content, enabled) VALUES(?,?,?,?)",
        (aid, emotion, content, enabled))
    conn.commit()
    return aid


class TestStyleAnchorPick:
    def test_exact_emotion_match(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors
        _seed_anchor(conn, "紧张", "刀光一闪，他后背瞬间绷紧。" * 10)
        picked = pick_style_anchors(conn, "紧张")
        assert len(picked) == 1
        assert picked[0]["emotion"] == "紧张"

    def test_fuzzy_emotion_match_bigram(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors
        _seed_anchor(conn, "紧张刺激", "内容" * 50)
        picked = pick_style_anchors(conn, "紧张")
        assert len(picked) == 1

    def test_no_match_returns_empty_fail_fast(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors
        _seed_anchor(conn, "悲怆", "内容" * 50)
        assert pick_style_anchors(conn, "扬眉吐气") == []   # 不退化为随便选

    def test_empty_emotion_returns_empty(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors
        _seed_anchor(conn, "紧张", "内容" * 50)
        assert pick_style_anchors(conn, "") == []
        assert pick_style_anchors(conn, None) == []

    def test_disabled_excluded_and_limit_2(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors
        _seed_anchor(conn, "紧张", "A" * 100, enabled=0)
        for i in range(3):
            _seed_anchor(conn, "紧张", f"B{i}" * 60)
        picked = pick_style_anchors(conn, "紧张")
        assert len(picked) == 2
        assert all("A" not in p["content"] for p in picked)

    def test_render_block_format(self, conn):
        from novelforge.craft.style_anchor import pick_style_anchors, render_anchor_block
        _seed_anchor(conn, "紧张", "刀光一闪。" * 20)
        block = render_anchor_block(pick_style_anchors(conn, "紧张"))
        assert "文风参考" in block and "禁止照搬" in block and "紧张" in block
        assert render_anchor_block([]) == ""


class TestStyleAnchorInjection:
    def test_draft_prompt_contains_anchor_block(self, conn):
        """chapter_draft user_msg 含锚点块；无锚点时不含。"""
        from novelforge.skills.chapter_draft_skill import ChapterDraftSkill
        from novelforge.control_plane.skill_base import SkillContext
        from novelforge.control_plane.budget import BudgetLedger
        from novelforge.control_plane.llm.gateway import LLMGateway
        from novelforge.control_plane.llm.fake_provider import FakeProvider

        captured = {}
        class _SpyProvider(FakeProvider):
            def generate(self, *a, **kw):
                # FakeProvider 签名以现有实现为准；捕获 messages 文本
                captured["msgs"] = a, kw
                return super().generate(*a, **kw)

        gw = LLMGateway(_SpyProvider(responses=[
            "```draft\n" + "正文" * 600 + "\n```\n```proposals\n[]\n```"]),
            BudgetLedger())
        ctx = SkillContext(
            "proj", 1, "auto_promote", 0, BudgetLedger(), gw, conn,
            workspace={
                "beats": [{"beat_type": "hook", "summary": "s"}],
                "style_anchor_block": "## 文风参考（仿其笔触与节奏，禁止照搬内容/人名/情节）\n【参考段 1】测试锚点",
            })
        ChapterDraftSkill().run(ctx)
        sent = str(captured["msgs"])
        assert "文风参考" in sent and "测试锚点" in sent

    def test_draft_prompt_clean_without_anchor(self, conn):
        from novelforge.skills.chapter_draft_skill import ChapterDraftSkill
        from novelforge.control_plane.skill_base import SkillContext
        from novelforge.control_plane.budget import BudgetLedger
        from novelforge.control_plane.llm.gateway import LLMGateway
        from novelforge.control_plane.llm.fake_provider import FakeProvider

        captured = {}
        class _SpyProvider(FakeProvider):
            def generate(self, *a, **kw):
                captured["msgs"] = a, kw
                return super().generate(*a, **kw)

        gw = LLMGateway(_SpyProvider(responses=[
            "```draft\n" + "正文" * 600 + "\n```\n```proposals\n[]\n```"]),
            BudgetLedger())
        ctx = SkillContext(
            "proj", 1, "auto_promote", 0, BudgetLedger(), gw, conn,
            workspace={"beats": [{"beat_type": "hook", "summary": "s"}]})
        ChapterDraftSkill().run(ctx)
        assert "文风参考" not in str(captured["msgs"])
```

（_SpyProvider 捕获方式以 FakeProvider 实际接口为准——如它有 `calls`/`last_messages` 记录则直接用，不必子类化。实现时先读 `novelforge/control_plane/llm/fake_provider.py`。）

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestStyleAnchorPick tests/test_p1_batch2.py::TestStyleAnchorInjection -x -q
```
预期：FAIL（ModuleNotFoundError: style_anchor）。

- [ ] **Step 3: 实现 craft/style_anchor.py**

```python
"""文风锚点选取与渲染（P1#9，oh-story §3.2.4）。

按本章 target_emotion 选 1-2 段 enabled 锚点做 few-shot：
精确情绪匹配 → bigram Jaccard ≥0.5 近似匹配 → 空（fail-fast 不瞎编）。
注入位置在 dynamic 段（draft/polish prompt），不碰稳定前缀（缓存无损）。
"""
from __future__ import annotations

import sqlite3
from typing import Optional

_FUZZY_THRESHOLD = 0.5
_LIMIT = 2


def pick_style_anchors(
    conn: sqlite3.Connection, emotion: Optional[str], limit: int = _LIMIT,
) -> list[dict]:
    """按情绪选锚点。无 emotion / 无匹配 → []（不退化为随机选段）。"""
    if not emotion:
        return []
    try:
        rows = conn.execute(
            "SELECT id, emotion, title, content FROM style_anchors"
            " WHERE enabled=1 ORDER BY created_at DESC").fetchall()
    except Exception:
        return []   # 老库无表 → 静默跳过
    if not rows:
        return []
    exact = [dict(r) for r in rows if r["emotion"] == emotion]
    if exact:
        return exact[:limit]
    # 近似：字符 bigram Jaccard（"紧张" vs "紧张刺激"）
    scored = [(s, dict(r)) for r in rows
              if (s := _similarity(emotion, r["emotion"])) >= _FUZZY_THRESHOLD]
    scored.sort(key=lambda x: -x[0])
    return [r for _, r in scored[:limit]]


def render_anchor_block(anchors: list[dict]) -> str:
    """渲染注入块；空列表返回空串（调用方据此决定是否拼接）。"""
    if not anchors:
        return ""
    lines = ["## 文风参考（仿其笔触与节奏，禁止照搬内容/人名/情节）"]
    for i, a in enumerate(anchors, 1):
        lines.append(f"【参考段 {i}】（情绪：{a['emotion']}）\n{a['content']}")
    return "\n".join(lines)


def _bigrams(s: str) -> set[str]:
    s = "".join(s.split())
    return {s[i:i + 2] for i in range(len(s) - 1)} if len(s) >= 2 else {s}


def _similarity(a: str, b: str) -> float:
    ba, bb = _bigrams(a), _bigrams(b)
    if not ba or not bb:
        return 0.0
    return len(ba & bb) / len(ba | bb)
```

- [ ] **Step 4: orchestrator 注入 + draft/polish 消费**

`orchestrator.py` RECALL 段（`workspace["dynamic_context"] = ...` 之后、PacingController 之前）：

```python
            # P1#9：文风锚点 few-shot——按本章细纲目标情绪选段，注入动态段。
            # 无情绪/无匹配锚点 → 空块（fail-fast 不瞎编）；不碰稳定前缀，缓存无损。
            from ..craft.style_anchor import pick_style_anchors, render_anchor_block
            emo_row = conn.execute(
                "SELECT target_emotion FROM chapter_cards WHERE chapter=?", (chapter,)
            ).fetchone()
            target_emotion = emo_row["target_emotion"] if emo_row else None
            workspace["style_anchor_block"] = render_anchor_block(
                pick_style_anchors(conn, target_emotion))
```

（import 移到文件头部已有 craft import 群里；老库无 target_emotion 列时 `conn.execute` 会抛 OperationalError——包一层 try/except 置 None。）

`chapter_draft_skill.py` 的 user_msg 组装改为：

```python
        anchor_block = ctx.workspace.get("style_anchor_block", "")
        user_msg = (
            f"{context_block}\n\n"
            + (f"{anchor_block}\n\n" if anchor_block else "")
            + f"## 本章任务\n"
            f"第 {ctx.target_chapter} 章\n"
            f"目标字数：约 {target_chars} 字\n"
            f"章节目标：{chapter_goal or '按情节发展'}\n\n"
            f"## Beat Sheet\n{beats_str}"
        )
```

（cache_hint 仍是 `len(stable)`——锚点块在稳定段之后，不影响前缀命中；同章多候选间块内容相同，候选间字节一致。）

`orchestrator.py::_polish` 全文润色分支的 user content 改为：

```python
        anchor_block = ctx.workspace.get("style_anchor_block", "")
        resp = ctx.llm.generate(
            ModelTier.MID,
            [Message(role="user", content=(
                f"{prefix}"
                + (f"{anchor_block}\n\n" if anchor_block else "")
                + f"工艺问题：\n{warns_str}\n\n当前草稿：\n{draft_text[:6000]}"
            ))],
            ...
```

（锚点补丁分支 generate_patches 不注入——补丁是局部修复，引入风格样本反而引导改写过多。）

- [ ] **Step 5: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py -q && .venv/bin/python -m pytest tests/test_m1.py tests/test_m3.py tests/test_p1_core.py -q
```
预期：PASS（m1 覆盖 draft prompt 缓存契约、p1_core 覆盖管线回归）。

- [ ] **Step 6: 提交**

```bash
git add novelforge/craft/style_anchor.py novelforge/control_plane/orchestrator.py novelforge/skills/chapter_draft_skill.py tests/test_p1_batch2.py
git commit -m "feat(P1#9): 文风锚点选取与注入——情绪匹配 fail-fast + draft/polish 动态段"
```

---

### Task 8: 爽点循环完成率（stats 现算）

**Files:**
- Modify: `novelforge/app/models.py`（ChapterStat.payoff_closed + PipelineStats.payoff_loop_rate）
- Modify: `novelforge/app/api/orchestrator.py`（pipeline_stats 计算）
- Test: `tests/test_p1_batch2.py`

- [ ] **Step 1: 写失败测试**

```python
class TestPayoffLoopRate:
    def _mk_completed_run(self, conn, chapter):
        conn.execute(
            "INSERT INTO pipeline_run(run_id, project_id, chapter, status, started_at)"
            " VALUES(?, 'proj', ?, 'completed', datetime('now'))",
            (f"run{chapter}", chapter))

    def test_three_evidence_sources_close_loop(self, conn):
        from novelforge.app.api.orchestrator import _payoff_closed_chapters
        from novelforge.ids import new_id
        # 章1：伏笔 payoff
        conn.execute(
            "INSERT INTO foreshadow(id, label, description, planted_chapter)"
            " VALUES('f1', 'l', 'd', 1)")
        conn.execute(
            "INSERT INTO foreshadow_log(id, foreshadow_id, chapter, action)"
            " VALUES('fl1', 'f1', 1, 'payoff')")
        # 章2：境界突破
        eid = new_id("ent")
        conn.execute("INSERT INTO entities(id, canonical_name, entity_type)"
                     " VALUES(?, '陆天', 'character')", (eid,))
        conn.execute("INSERT INTO power_ranks(id, system_name, rank_name, rank_order)"
                     " VALUES('pr1', '修真', '筑基', 2)")
        conn.execute(
            "INSERT INTO character_power_log(id, entity_id, system_name, rank_id,"
            " rank_order, change_chapter, change_type)"
            " VALUES('cpl1', ?, '修真', 'pr1', 2, 2, 'breakthrough')", (eid,))
        # 章3：获得道具
        iid = new_id("ent")
        conn.execute("INSERT INTO entities(id, canonical_name, entity_type)"
                     " VALUES(?, '玄铁剑', 'item')", (iid,))
        conn.execute(
            "INSERT INTO item_log(id, item_entity_id, quantity_delta,"
            " change_chapter, change_type) VALUES('il1', ?, 1, 3, 'acquire')", (iid,))
        # 章4：injury_drop 不算爽点
        conn.execute(
            "INSERT INTO character_power_log(id, entity_id, system_name, rank_id,"
            " rank_order, change_chapter, change_type)"
            " VALUES('cpl2', ?, '修真', 'pr1', 1, 4, 'injury_drop')", (eid,))
        conn.commit()
        closed = _payoff_closed_chapters(conn)
        assert closed == {1, 2, 3}

    def test_stats_payload_has_rate(self, conn):
        """完成率 = 闭环章 ∩ 完成章 / 完成章。"""
        from novelforge.app.api.orchestrator import _payoff_closed_chapters
        assert _payoff_closed_chapters(conn) == set()
```

（power_ranks 的列名以 schema 为准——实现前 `grep -n "CREATE TABLE.*power_ranks" -A 8 novelforge/db/schema.sql` 核对，FK 不满足时调整种子。pipeline_run 的必填列同样核对后调整 `_mk_completed_run`。）

再加一个 API 层断言（挂进现有 TestStyleAnchorApi 的 client fixture 复用）：

```python
    def test_stats_endpoint_exposes_payoff_fields(self, client):
        c, pid = client
        r = c.get(f"/v1/{pid}/pipeline/stats")
        body = r.json()
        assert "payoff_loop_rate" in body
```

- [ ] **Step 2: 跑测试确认失败**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py::TestPayoffLoopRate -x -q
```
预期：FAIL（ImportError: _payoff_closed_chapters）。

- [ ] **Step 3: 实现**

`models.py`：

```python
class ChapterStat(BaseModel):
    chapter: int
    word_count: Optional[int] = None
    quality_score: Optional[float] = None
    finished_at: Optional[str] = None
    tokens_spent: Optional[int] = None   # 逐章成本（v11）
    usd_spent: Optional[float] = None
    payoff_closed: bool = False          # P1#10: 本章有确定性爽点闭环证据
```

`PipelineStats` 加：

```python
    payoff_loop_rate: Optional[float] = None   # P1#10: 闭环章/完成章（≥0.7 高 0.5-0.7 中 <0.5 低）
```

`app/api/orchestrator.py` 模块级新函数 + pipeline_stats 接线：

```python
def _payoff_closed_chapters(conn) -> set[int]:
    """P1#10 爽点闭环章集合（纯确定性证据，零 LLM）。

    任一即闭环：伏笔 payoff / 正向实力变动（breakthrough/unseal）/ 道具入手（acquire/craft）。
    injury_drop/seal/init、transfer/consume/destroy/lose 不算爽点。
    """
    closed: set[int] = set()
    for sql in (
        "SELECT DISTINCT chapter AS c FROM foreshadow_log WHERE action='payoff'",
        "SELECT DISTINCT change_chapter AS c FROM character_power_log"
        " WHERE change_type IN ('breakthrough','unseal')",
        "SELECT DISTINCT change_chapter AS c FROM item_log"
        " WHERE change_type IN ('acquire','craft')",
    ):
        try:
            closed |= {r["c"] for r in conn.execute(sql).fetchall()}
        except Exception:
            continue   # 老库缺表 → 该证据源退出口径
    return closed
```

pipeline_stats 中 series 构建改为：

```python
        closed = _payoff_closed_chapters(conn)
        series = [ChapterStat(
            chapter=r["chapter"], word_count=r["word_count"],
            quality_score=r["quality_score"], finished_at=r["finished_at"],
            tokens_spent=r["tokens_spent"], usd_spent=r["usd_spent"],
            payoff_closed=r["chapter"] in closed,
        ) for r in rows]
```

返回值加：

```python
            payoff_loop_rate=(round(sum(1 for s in series if s.payoff_closed) / len(series), 3)
                              if series else None),
```

- [ ] **Step 4: 跑测试确认通过**

```bash
.venv/bin/python -m pytest tests/test_p1_batch2.py -q
```
预期：PASS。

- [ ] **Step 5: 提交**

```bash
git add novelforge/app/models.py novelforge/app/api/orchestrator.py tests/test_p1_batch2.py
git commit -m "feat(P1#10): 爽点循环完成率——三证据源确定性口径，stats 现算"
```

---

### Task 9: 前端最小集（完成率徽章 + 逐章标记 + 章节卡新字段透出）

**Files:**
- Modify: `web/src/api/types.ts`（ChapterStat/PipelineStats/ChapterCard 加字段）
- Modify: `web/src/components/studio/Studio.tsx`（质量趋势区徽章 + 逐章 bar tooltip）

- [ ] **Step 1: types.ts 加字段**

`ChapterStat` 加 `payoff_closed?: boolean;`；`PipelineStats` 加 `payoff_loop_rate?: number | null;`；章节卡类型（若有）加 `target_emotion?/opening_hook_type?/hook_type?/expectation_score?`。

- [ ] **Step 2: Studio.tsx 质量趋势头部加徽章**

在 `📈 质量趋势` 的 `ph-hint` span 内、低分警告之前插入：

```tsx
              {stats.payoff_loop_rate != null && (
                <span
                  title={`爽点循环完成率：有确定性爽点闭环证据（伏笔回收/突破/得宝）的章数占比。≥70% 高 / 50-70% 中 / <50% 低`}
                  style={{
                    color: stats.payoff_loop_rate >= 0.7 ? '#4ade80'
                      : stats.payoff_loop_rate >= 0.5 ? '#fbbf24' : '#f87171',
                  }}
                >
                  　⚡ 爽点闭环 {Math.round(stats.payoff_loop_rate * 100)}%
                </span>
              )}
```

逐章质量 bar 的 title 加闭环标记（在现有 title 模板字符串尾部追加）：

```tsx
title={`第 ${s.chapter} 章：…${s.payoff_closed ? ' · ⚡爽点闭环' : ''}`}
```

并给闭环章的 bar 加一个视觉提示——底部 2px 描边（不动主色，区别于低分红色）：

```tsx
boxShadow: s.payoff_closed ? 'inset 0 -2px 0 rgba(250,204,21,0.9)' : undefined,
```

- [ ] **Step 3: 构建验证**

```bash
cd /Users/nasa1024/workspace/zi/web && npm run build
```
预期：构建通过无 TS 错误。

- [ ] **Step 4: 提交**

```bash
cd /Users/nasa1024/workspace/zi
git add web/src/api/types.ts web/src/components/studio/Studio.tsx
git commit -m "feat(P1#10): 前端最小集——爽点闭环率徽章 + 逐章闭环标记"
```

---

### Task 10: 收尾——全量回归 + 合入 main

- [ ] **Step 1: 全量测试**

```bash
cd /Users/nasa1024/workspace/zi && .venv/bin/python -m pytest -q
```
预期：全 PASS（上一批基线 397，本批新增约 25-30 条）。

- [ ] **Step 2: 前端构建**

```bash
cd /Users/nasa1024/workspace/zi/web && npm run build
```
预期：通过。

- [ ] **Step 3: 合入 main（沿用本地直合偏好）**

```bash
cd /Users/nasa1024/workspace/zi
git checkout main && git merge --no-ff feat/p1-batch2 -m "Merge feat/p1-batch2: P1 第二批（细纲契约 / 文风锚点 / 爽点循环完成率）"
git branch -d feat/p1-batch2
.venv/bin/python -m pytest -q
```
预期：合并后全绿。

---

## Self-Review 备忘（计划级）

- spec §1.2 钩子枚举 → Task 2；§1.3 生产 → Task 3；§1.4 消费 → Task 4；§1.5 检查 → Task 5。
- spec §2.1 表 → Task 1；§2.2 API → Task 6；§2.3 注入 → Task 7。
- spec §3 完成率 → Task 8；§3.3 前端 → Task 9。
- 实现中两处「以现有代码为准」点：FakeProvider 捕获接口（Task 7 Step 1）、pipeline_run/power_ranks 必填列（Task 8 Step 1）——执行时先读后写，不改契约只调种子写法。
- v13 必须处理 chapter_cards 缺表路径（同 v12 foreshadow 的坑）。
