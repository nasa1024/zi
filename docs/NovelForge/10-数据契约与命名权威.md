## 10. 数据契约与命名权威

> 本节是 NovelForge 全文档对 **9 处跨节冲突表/契约定义**的**唯一权威源（Single Source of Truth for Schema）**。凡 §02/§03/§06/§08 与本节冲突者，**一律以本节为准**；冲突节须按"命名权威表"末列改写为"引用 §10"，不得各自保留旧定义。
>
> 本节只裁定**契约形态**(DDL 列 / 枚举 CHECK / Pydantic 字段 / 拼写 / 节号引用)，不重复业务算法——晋升决策算法见 §03，validator 实现见 §04，召回排序见 §06，API 契约见 §08。
>
> 立场:命名与 schema 的分裂是连写系统最隐蔽的漂移源(外键类型不匹配会静默断链、状态机词汇不一致会让治理逻辑分叉、字段名拼写差一字会让 validator 读到 NULL)。本节用一处权威把它们焊死。

---

### 10.1 SECTION_MAP:节号权威映射表

全文档**所有**"详见第 X 节 / 见 §X"交叉引用,**必须**使用下表的实际节号(01-11)。**禁止**任何节内部使用假想编号(如旧 §03 写"晋升详见第 6 节"、旧 §06 写"晋升闸门详见第 9 节"、旧 §08 写"详见第 3-7 节"等,全部作废)。

| 节号 | 节名 | 权威职责(该主题以此节为准) |
|---|---|---|
| 01 | 总体架构 | 三平面划分、11 条硬原则、主循环总图 |
| 02 | 存储与数据模型 | **全部 DDL / Pydantic / 真相源 schema 的权威**(facts/fact_revisions/World State 表/工艺层表/检索表/promotion_log 单一 DDL) |
| 03 | Canon治理与双模式 | 晋升 / PromotionPolicy / Route 决策 / review_queue / 双模式分叉 / retcon / revert |
| 04 | 一致性引擎 | validator / as-of 投影消费 schema / state_reachable |
| 05 | 网文工艺层 | beat / PacingController / voice / value_shift 属性 |
| 06 | 记忆管线与召回 | capture / extract(L0→L1→L2) / Recall 多通道 / 检索使用 / prompt cache / 分层预算 |
| 07 | Skill体系与主循环 | Skill / Workspace / circuit breaker / budget / 主循环编排 |
| 08 | API配置与工程落地 | FastAPI 端点 / config.yaml / 目录结构 / 技术选型 / 依赖清单 |
| 09 | MVP路线图与验收 | MVP 分期、验收标准 |
| **10** | **数据契约与命名权威** | **本节:9 处冲突的唯一裁定 + 命名权威表 + SECTION_MAP** |
| 11 | 核心算法补全 | evidence_strength 打分、RRF、networkx 视图构建等算法补全 |

> 主题 → 节速查:World State / 境界 / 知情者图 / 时间线 = **§02**(表定义) + **§04**(校验);治理 / 晋升 = **§03**;检索 / 召回 / 缓存 = **§06**;validator = **§04**;工艺 / beat = **§05**;API / config = **§08**;主循环 = **§07**;MVP = **§09**。

---

### 10.2 命名权威表(被收敛项 → 权威定义 → 哪些节须改)

| # | 被收敛项 | 权威定义(最小形态) | 须改的节(改为引用 §10) |
|---|---|---|---|
| R1 | 交叉引用节号 | 一律用 §10.1 SECTION_MAP 的 01-11 实际节号 | §02/§03/§06/§08 内**所有**"详见第 X 节"逐句核对改正 |
| R2 | `facts` 主键及 fact 引用 ID 类型 | **`TEXT` 前缀化 ID**(`fact_000123` / `fact_pw_lin`);所有指向 fact 的外键同为 `TEXT` | §03(把 `facts.fact_id INTEGER` 及 `target_id:int`/`entity_id:int`/外键全改 `TEXT`) |
| R3 | `fact_candidates` 主键 + 候选状态机 | 主键 `candidate_id TEXT`(`cand_xxx`);状态机唯一 = `proposed → pending_review → (promoted \| rejected \| superseded)` | §02(主键 `id`→`candidate_id`、状态集改)、§03(`INTEGER PK`→`TEXT`、去 `canon`)、§08(去 `approved`/`committed`) |
| R4 | `facts.status`(事实自身状态) | `{canon, tentative, retconned}`,与候选状态严格区分(候选用 `promoted`,事实用 `canon`) | 全节统一,严禁把 `promoted` 用于 facts、`canon` 用于 candidates |
| R5 | PromotionPolicy 决策枚举 | `Route = {COMMIT, REVIEW, HOLD, REJECT}` | §03(原两态 `COMMIT/REVIEW` 扩为四态)、§07(原 `STAGING/REVIEW/COMMIT` 对齐) |
| R6 | 全文检索表拆分 | `facts_fts`(canon facts 文本) + `drafts_fts`(L0 草稿正文行);均 `unicode61` + 应用层 jieba 预分词;可选 `facts_fts_tri`(trigram) | §06(草稿上的 FTS 表 `facts_fts`→**`drafts_fts`**)、§08(删除第三套混合定义,采纳本拆分) |
| R7 | `promotion_log` 单一 DDL + changelog | 单一权威 DDL 放 §02(并采纳 §03 的 `reverts_log_id` 等补充字段);**`canon_changelog` 取消**,内容变更流由 `fact_revisions` 承担 | §03(不再重建 `promotion_log`,改"引用 §02/§10";删 `canon_changelog` 提法)、§08(删 `canon_changelog`) |
| R8 | `BibleChangeProposal` 契约 | **单一 Pydantic 契约**(权威字段见 §10.3-R8);其余节统一引用 | §02/§03/§06/§08 各自的 `BibleChangeProposal` 定义全部删除,改引用 §10 |
| R9 | `beats.beat_type` 取值域 | `{setup, turn, payoff_beat, tension_point, hook}`;`value_shift` 改为 beat 属性 `value_start`/`value_end` | §02(`beat_type` CHECK 改、新增 value 列)、§05(`rising/tension/payoff` 重命名对齐) |
| R10 | 知情者图项 `require_human_for` 拼写 | 统一 = **`knowledge_edge_change`** | §01(`knowledge_graph_change`→改)、§08(`knowledge_edge`→改) |
| R11 | `knowledge_edges` DDL 列 | 增 `public_from_chapter INTEGER NULL` + `secrecy_level TEXT`;`is_public(info,ch)` = `public_from_chapter IS NOT NULL AND public_from_chapter <= ch` | §02(加两列)、§04(validator 引用的字段从此真实存在) |

---

### 10.3 逐条权威落定定义(R2-R11)

每条给出 **(a) 最终落定定义**(完整 DDL / 枚举 CHECK / Pydantic 类) + **(b) 为何这样裁定**。

---

#### R2 — `facts` 主键及所有 fact 引用统一 `TEXT` 前缀化 ID

**(a) 权威定义**

`facts.id` 为 `TEXT PRIMARY KEY`,值为应用层生成的前缀化 ID(`fact_` + ULID/序号,如 `fact_000123`、`fact_pw_lin`)。**所有**指向 fact 的列(无论叫 `fact_id` / `target_fact_id` / `target_id` / `committed_fact_id` / `secret_fact_id` / `current_revision_id` 所指对象等)一律 `TEXT`,外键类型与 `facts.id` 严格一致。

```sql
-- §02 权威保持不变(facts.id 已是 TEXT);此处声明所有引用列的类型铁律:
-- facts.id                         TEXT PRIMARY KEY          -- fact_xxx
-- fact_revisions.fact_id           TEXT NOT NULL REFERENCES facts(id)
-- fact_candidates.target_fact_id   TEXT REFERENCES facts(id)
-- fact_candidates.committed_revision_id TEXT                 -- → fact_revisions.id (rev_xxx)
-- promotion_log.fact_id            TEXT REFERENCES facts(id)
-- knowledge_edges.secret_fact_id   TEXT REFERENCES facts(id)
-- power_ranks/character_power_log/item_log/...的 fact_id 列  TEXT
```

Pydantic 侧(权威):

```python
class Fact(BaseModel):
    id: str                                  # fact_xxx,前缀化 TEXT(禁 int)
    entity_id: Optional[str] = None          # ent_xxx,TEXT
    current_revision_id: str                 # rev_xxx,TEXT
    # ...其余见 §02.2.9
```

**§03 必改项**:把 §03.2 `fact_candidates` DDL 中 `candidate_id INTEGER`、`entity_id INTEGER`、`target_fact_id INTEGER`、`committed_fact_id INTEGER` 及 `promotion_log` 中 `candidate_id/target_fact_id/entity_id INTEGER`、§03.1 `BibleChangeProposal` 中 `target_id: Optional[int]` / `entity_id: Optional[int]` **全部改 `TEXT` / `Optional[str]`**(外键随之)。

**(b) 为何这样裁定**

§02 通用列约定第 1 条已明文规定"主键统一 `id TEXT PRIMARY KEY`,前缀化 ID,便于跨表引用与日志可读";§02 的 `facts.id`、`fact_revisions.fact_id`、`fact_candidates.target_fact_id` 全是 `TEXT`。唯独 §03 用了 `INTEGER PRIMARY KEY AUTOINCREMENT`,与 §02 直接冲突——同一外键两种类型会让 SQLite 隐式转换、`REFERENCES` 校验形同虚设,并破坏"日志可读"(整数 ID 无前缀无法肉眼判断是 fact 还是 candidate)。§02 是 DDL 权威节(SECTION_MAP),故收敛到 `TEXT` 前缀化。

---

#### R3 — `fact_candidates` 主键 + 候选状态机

**(a) 权威定义**

主键 `candidate_id TEXT`(`cand_xxx`)。候选状态机**唯一**为:

```
proposed → pending_review → (promoted | rejected | superseded)
```

- `proposed`:LLM 刚产出、未经 PromotionPolicy 裁定(唯一入口态)
- `pending_review`:已入 `review_queue`,等待人审(由 Route=REVIEW 落入)
- `promoted`:**已离开 staging 进入 canon**(终态;由 Route=COMMIT 或人审 approve 达成)
- `rejected`:被拒(终态)
- `superseded`:被后续 retcon 取代(终态)

**禁用** `approved` / `committed` / `canon` 作候选状态(候选只说"已晋升 = `promoted`",事实才说 `canon`,见 R4)。

权威 CHECK:

```sql
CREATE TABLE IF NOT EXISTS fact_candidates (
    candidate_id    TEXT PRIMARY KEY,                 -- cand_xxx (R2/R3:TEXT)
    -- ...(其余列见 §02.2.3,target_fact_id/entity_id 均 TEXT)
    status          TEXT NOT NULL DEFAULT 'proposed'
        CHECK (status IN ('proposed','pending_review','promoted','rejected','superseded')),
    committed_revision_id TEXT,                        -- 晋升后回填 fact_revisions.id
    -- ...
    FOREIGN KEY(target_fact_id) REFERENCES facts(id)
);
```

合法迁移(每条边落 `promotion_log`):

| from | to | 触发 |
|---|---|---|
| `proposed` | `promoted` | Route=COMMIT(auto 低风险无冲突) |
| `proposed` | `pending_review` | Route=REVIEW(human_gate / 高风险 / 冲突 / 证据不足) |
| `proposed` | `superseded` | 入 staging 即被更新候选取代(罕见) |
| `pending_review` | `promoted` | 人审 approve |
| `pending_review` | `rejected` | 人审 reject |
| `pending_review`/`promoted` | `superseded` | 后续 retcon |

Route=HOLD 不迁移状态:候选**留在 `proposed`** 等证据积累(见 R5)。

Pydantic 侧:

```python
class FactCandidate(BaseModel):
    candidate_id: str                                 # cand_xxx
    status: Literal["proposed","pending_review","promoted","rejected","superseded"] = "proposed"
    target_fact_id: Optional[str] = None
    committed_revision_id: Optional[str] = None
    # ...见 §02.2.9
```

**(b) 为何这样裁定**

三节给了三套不兼容状态机:§02 = `proposed/approved/rejected/committed/superseded`(`id` 为 PK)、§03 = `proposed/pending_review/canon/rejected/superseded`(`candidate_id INTEGER` PK)、§06 = `proposed/promoted/rejected`、§08 = `proposed/approved/rejected/superseded`。问题:① §03 把 `canon` 当候选状态,与事实自身状态(R4)语义重叠,治理代码无法区分"这是候选记录还是真相记录";② `approved` 与 `committed` 双态冗余(approve 后必 commit,中间态无落库价值,反而制造"approved 但未 committed"的悬空窗口);③ 主键类型分裂(R2)。裁定取 §03 的 `pending_review`(它精确表达"在 review_queue 里")+ §06 的 `promoted`(它精确表达"已离 staging 进 canon",且不与事实的 `canon` 撞词),删掉 `approved`/`committed`/`canon`。主键统一 `candidate_id TEXT` 兼顾 §03 的列名习惯与 §02 的 TEXT 铁律。

---

#### R4 — `facts.status`(事实表自身状态)

**(a) 权威定义**

```sql
status TEXT NOT NULL DEFAULT 'tentative'
    CHECK(status IN ('canon','tentative','retconned'))
```

语义:`tentative`(暂定,未晋升)、`canon`(已晋升生效的真相)、`retconned`(被 retcon 逻辑失效,物理保留)。

```python
class FactStatus(str, Enum):
    canon = "canon"
    tentative = "tentative"
    retconned = "retconned"
```

**与 R3 的硬边界(铭记)**:

| 概念 | 用词 | 表 |
|---|---|---|
| 候选"已晋升离开 staging" | **`promoted`** | `fact_candidates.status` |
| 事实"是当前生效真相" | **`canon`** | `facts.status` |
| 事实"被取代失效" | **`retconned`** | `facts.status` |
| 候选"被取代" | **`superseded`** | `fact_candidates.status` |

`promoted ≠ canon`,`superseded ≠ retconned`——前者描述候选记录的治理生命周期,后者描述事实记录的真相生命周期。一次成功晋升 = 候选 `promoted` **且** 新事实 `canon`(同一事务两处状态)。

**§03 必改项**:§03.2 状态机表与 mermaid 图里"`proposed → canon`"的箭头,应理解为"候选 `proposed→promoted`,同事务写入一条 `status=canon` 的 fact",不可把 `canon` 写进 `fact_candidates.status` 的 CHECK(已在 R3 删除)。§03.7 retcon 把旧 fact 置 `superseded` 的说法**改为 `retconned`**(事实失效用 `retconned`,§02 facts CHECK 无 `superseded`)。

**(b) 为何这样裁定**

§02 facts CHECK 是 `{canon, tentative, retconned}`,而 §03.7 retcon 段落写"旧 `facts.status` 由 `canon` 置为 `superseded`"——但 §02 的 facts 根本没有 `superseded` 这个合法值,该 UPDATE 会被 CHECK 拒绝。`superseded` 是候选词汇(R3),事实失效的词汇是 `retconned`。两套生命周期(候选治理 vs 事实真相)必须用两组互不重叠的词,否则"promoted 的候选对应的 fact 到底该查 status=promoted 还是 canon"会让每个查询都要猜。§02 为 DDL 权威,facts 三态不动,retcon 统一写 `retconned`。

---

#### R5 — PromotionPolicy 决策枚举 `Route`

**(a) 权威定义**

```python
class Route(str, Enum):
    COMMIT = "commit_canon"     # 直接写 canon:候选 →promoted,新 fact →canon
    REVIEW = "enqueue_review"   # 入 review_queue 等人审:候选 →pending_review
    HOLD   = "hold_staging"     # 留 staging 待证据积累:候选维持 proposed,不入队不写 canon
    REJECT = "reject"           # 拒:候选 →rejected
```

四态语义与副作用:

| Route | 含义 | 候选状态迁移 | 落账 op | 典型触发 |
|---|---|---|---|---|
| `COMMIT` | commit_canon | `proposed`→`promoted` | `commit_canon` | auto/hybrid,低风险 + 无冲突 + 证据达阈 |
| `REVIEW` | enqueue_review | `proposed`→`pending_review` | `enqueue_review` | human_gate / 命中 require_human_for / 高风险 / 有冲突 |
| `HOLD` | 留 staging | `proposed`(不变) | `hold_staging` | 证据不足阈值但**未矛盾**,等后续章补证据再裁(避免过早拒绝可能正确的提案) |
| `REJECT` | 拒 | `proposed`/`pending_review`→`rejected` | `reject` | 人审否决 / 确定性硬冲突无法 retcon |

`PromotionPolicy.decide()` 返回 `Route`;`promotion_log.decision` 的 CHECK 同步为四态:

```sql
-- promotion_log.decision(权威 DDL 在 §02,见 R7)
decision TEXT NOT NULL
    CHECK(decision IN ('commit_canon','enqueue_review','hold_staging','reject','revert'))
    -- revert 为单条回滚专用,不由 decide() 产出
```

**(b) 为何这样裁定**

§03.3 `Decision` 只有两态 `{COMMIT, REVIEW}`,§07 用 `{STAGING, REVIEW, COMMIT}` 三态。两者都缺"明确拒绝"(REJECT)与"留置等证据"(HOLD)的独立语义:§03 把"证据不足"也塞进 REVIEW(让人审去拒),把队列塞满本可自动留置的低证据项;§07 的 STAGING≈HOLD 但缺 REJECT。统一为四态 `{COMMIT, REVIEW, HOLD, REJECT}`:HOLD 收敛 §07 的 STAGING(证据不足但未矛盾 → 留 proposed,不打扰人),REJECT 显式化"确定性硬冲突/人审否决"。这让决策点对每种处境都有精确出口,且 `promotion_log.decision` 能 1:1 审计。`revert` 不是 decide() 的产物(它是事后回滚,§03.7),但同表枚举需容纳。

---

#### R6 — 全文检索拆两张表:`facts_fts` 与 `drafts_fts`

**(a) 权威定义**

两张独立 FTS5 表,职责分明;均 `unicode61` + **应用层 jieba 预分词**(写入前 `jieba.lcut` 空格 join,查询端同口径切词);`tokenizer_version` 入 `meta_kv`。可选第三张 `facts_fts_tri`(trigram)做人名/术语子串兜底。

```sql
-- (1) facts_fts:索引 canon facts 的可检索文本列(权威在 §02.8.1)
CREATE VIRTUAL TABLE IF NOT EXISTS facts_fts USING fts5(
    fact_id    UNINDEXED,        -- 回连 facts.id
    subject_tok,                 -- jieba 预分词:主体
    predicate_tok,               -- jieba 预分词:谓词
    object_tok,                  -- jieba 预分词:值
    detail_tok,                  -- jieba 预分词:detail_json 可读文本
    tokenize = "unicode61 remove_diacritics 2"
);

-- (2) drafts_fts:索引 L0 草稿正文行,用于"查相似桥段 / 原文检索"
CREATE VIRTUAL TABLE IF NOT EXISTS drafts_fts USING fts5(
    draft_id   UNINDEXED,        -- 回连 draft_index.id / drafts.draft_id
    chapter_no UNINDEXED,
    line_start UNINDEXED,        -- source_ref 行区间回指
    line_end   UNINDEXED,
    body,                        -- jieba 预分词后的草稿正文行区间文本
    tokenize = "unicode61 remove_diacritics 2"
);

-- (3) 可选:facts_fts_tri(trigram 子串兜底,人名/术语精确子串)
CREATE VIRTUAL TABLE IF NOT EXISTS facts_fts_tri USING fts5(
    fact_id UNINDEXED, content, tokenize = 'trigram'
);
```

`tokenize` 一律 `unicode61`(因输入已是空格分隔词串);**禁止** `tokenize='jieba'` 这种"FTS5 内置 jieba 分词器"写法(jieba 在应用层做,FTS5 只按空白切)。

**§06 必改项**:§06.1 `capture()` 与 DDL 中建在 L0 草稿上的 `facts_fts`(含 `draft_id/chapter_no/line_start/line_end/body` 列)**全部改名 `drafts_fts`**;§06.4.2 `recall_bm25` 中 `FROM facts_fts`(查草稿原文)改 `FROM drafts_fts`。§06 中 `tokenize='jieba'` 改为 `unicode61` + 注明应用层预分词。

**§08 必改项**:§08.5 把"`facts_fts(content)` 单表混装 + 注释含糊"那套删除,采纳本节两表拆分;`facts_fts_tri` 保留为可选兜底。

**(b) 为何这样裁定**

三套定义打架:§02.8.1 `facts_fts` 索引的是 **facts 的结构化列**(`subject_tok/predicate_tok/...`);§06.1 的 `facts_fts` 索引的却是 **L0 草稿正文行**(`body/line_start/line_end`);§08.5 又是第三套(`facts_fts(fact_id, entity, content)` + `facts_fts_tri`)。同名表三种 schema,建表必崩。本质是**两个不同检索对象**被错塞进一个名字:canon 事实检索(给 Recall 主路补充关键词)vs 草稿原文检索(给作者"查相似桥段/上次怎么写的")。必须拆名:facts 的留 `facts_fts`(§02 权威),草稿的独立成 `drafts_fts`。jieba 统一在应用层(§02.8.1 已论证 FTS5 无内置中文分词器,且词典版本需绑 `meta_kv`),故 `tokenize` 恒为 `unicode61`,§06 的 `tokenize='jieba'` 是不可实现的伪写法,删除。

---

#### R7 — `promotion_log` 单一权威 DDL + `canon_changelog` 取消

**(a) 权威定义**

`promotion_log` 单一权威 DDL **放 §02**,采纳 §03 的补充字段(`reverts_log_id` 等),主键与外键遵守 R2(TEXT):

```sql
-- promotion_log:晋升决策 append-only 审计(唯一权威 DDL,落 §02.2.4)
CREATE TABLE IF NOT EXISTS promotion_log (
    id              TEXT PRIMARY KEY,                 -- plog_xxx (R2:TEXT)
    candidate_id    TEXT,                             -- → fact_candidates(candidate_id)
    fact_id         TEXT,                             -- → facts(id),命中的 canon 事实
    entity_id       TEXT,                             -- 便于按 entity 拉治理时间线
    decision        TEXT NOT NULL
        CHECK(decision IN ('commit_canon','enqueue_review','hold_staging','reject','revert')),  -- R5
    policy_mode     TEXT NOT NULL
        CHECK(policy_mode IN ('human_gate','auto_promote','hybrid')),
    risk_tier       TEXT,
    evidence_strength REAL,
    chapter         INTEGER,                          -- valid_from_chapter / source_chapter
    conflict_summary TEXT,                            -- JSON
    old_value       TEXT,                             -- JSON,revert/update/retcon 留痕
    new_value       TEXT,                             -- JSON
    reason          TEXT NOT NULL,
    actor           TEXT NOT NULL,                    -- 'system:policy' / 'human:<user>' / 'revert:<user>'
    reverts_log_id  TEXT,                             -- revert 专用:指向被回滚的 promotion_log.id
    created_at      TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY(candidate_id)   REFERENCES fact_candidates(candidate_id),
    FOREIGN KEY(fact_id)        REFERENCES facts(id),
    FOREIGN KEY(reverts_log_id) REFERENCES promotion_log(id)
);
CREATE INDEX IF NOT EXISTS idx_plog_candidate ON promotion_log(candidate_id);
CREATE INDEX IF NOT EXISTS idx_plog_fact      ON promotion_log(fact_id);
CREATE INDEX IF NOT EXISTS idx_plog_entity    ON promotion_log(entity_id, created_at);
CREATE INDEX IF NOT EXISTS idx_plog_time      ON promotion_log(created_at);

-- append-only 兜底触发器(禁 UPDATE/DELETE,见 §03.4)
CREATE TRIGGER IF NOT EXISTS trg_plog_no_update
  BEFORE UPDATE ON promotion_log BEGIN
    SELECT RAISE(ABORT, 'promotion_log 是 append-only,禁止 UPDATE'); END;
CREATE TRIGGER IF NOT EXISTS trg_plog_no_delete
  BEFORE DELETE ON promotion_log BEGIN
    SELECT RAISE(ABORT, 'promotion_log 是 append-only,禁止 DELETE'); END;
```

**`canon_changelog` 取消**:不存在该表。**内容变更流(fact 值如何演化)职责由 `fact_revisions`(§02.2.2)承担**;治理动作流(谁在什么模式下做了什么裁定)由 `promotion_log` 承担。二者均 append-only,可按 `fact_id`/`entity_id` 交叉对账。

**§03 必改项**:§03.4 不再重复建 `promotion_log` DDL,改为"`promotion_log` 单一权威 DDL 见 §02 / §10,本节只描述其约束(append-only、reverts_log_id 回滚链)"。§03.4 末尾"与 `canon_changelog`(详见第 2 节)互补"一句**删除**,改为"与 `fact_revisions`(§02)互补"。§03.9 产出清单里 `canon_changelog` 删除。

**§08 必改项**:§08.1 第 25 行"落到 append-only 表(`fact_revisions`、`promotion_log`、`canon_changelog`)"删 `canon_changelog`;§08.4 目录 `governance/audit.py` 注释"`promotion_log / canon_changelog`"删 `canon_changelog`。

**(b) 为何这样裁定**

§02.2.4 与 §03.4 各建了一张 `promotion_log`,字段集不同(§02 有 `risk_tier/evidence_strength/conflict_summary`,§03 有 `entity_id/chapter/reverts_log_id/ts`),主键类型也不同(R2)。两张同名表 = 建表冲突。合并:以 §02(DDL 权威节)为基,并入 §03 独有的 `entity_id/chapter/reverts_log_id`(回滚链与按 entity 对账确实需要)。至于 `canon_changelog`:§03.4/§08 反复提它"记内容变更流",但 `fact_revisions`(§02.2.2)已经是"append-only 完整修订历史(old_object/new_object/op/valid_from_chapter)"——这正是内容变更流,`canon_changelog` 是职责重复的影子表,留着只会双写漂移。删除,职责归 `fact_revisions`。

---

#### R8 — `BibleChangeProposal` 单一 Pydantic 契约

**(a) 权威定义(权威在 §10,其余节引用)**

```python
from typing import Literal, Optional
from pydantic import BaseModel, Field, model_validator, field_validator

class BibleChangeProposal(BaseModel):
    op: Literal["add", "update", "deprecate", "retcon"]
    target_id: Optional[str] = None        # add 时为 None;update/deprecate/retcon 必填,指向 facts.id (R2:TEXT)
    entity: str                            # 该 fact 归属实体的稳定标识(canonical_name 或 ent_xxx)
    fact_type: str                         # world_rule|power_system|character_trait|... (取值见 §02 facts CHECK)
    old: Optional[dict] = None             # 旧值快照(add 时 None;update/deprecate/retcon 回填,供审计与回滚)
    new: dict                              # 新值(结构化,非自然语言);deprecate 也需给终态快照
    reason: str = Field(min_length=1)      # 变更理由(供审校与审计)
    evidence_refs: list[str] = Field(default_factory=list)  # 出处:chap:42#para:7 / fact_xxx / draft 行区间,可程序校验
    valid_from_chapter: int = Field(ge=0)  # canon 绑定的生效章(故事内时序)
    risk_class: str                        # 由 classify_risk 填:world_rule|power_system|character_death|foreshadow_payoff|knowledge_edge_change|soft

    @model_validator(mode="after")
    def _op_target_consistency(self):
        if self.op in ("update", "deprecate", "retcon") and not self.target_id:
            raise ValueError(f"op={self.op} 必须提供 target_id")
        if self.op == "add" and self.target_id:
            raise ValueError("op=add 不应携带 target_id")
        return self

    @field_validator("evidence_refs")
    @classmethod
    def _need_evidence(cls, v: list[str]):
        if not v:
            raise ValueError("evidence_refs 不可为空:晋升依据=可程序校验的 evidence_strength(硬原则8)")
        return v
```

字段权威清单(其余节不得增删改名):

| 字段 | 类型 | 规则 |
|---|---|---|
| `op` | `Literal["add","update","deprecate","retcon"]` | 四值;`retcon` 显式表达"改设定取代旧 fact" |
| `target_id` | `Optional[str]` | add=None;其余必填,TEXT 指向 `facts.id` |
| `entity` | `str` | 归属实体标识 |
| `fact_type` | `str` | 取 §02 facts.fact_type CHECK 集 |
| `old` | `Optional[dict]` | add=None;其余回填旧值快照 |
| `new` | `dict` | 必填(deprecate 给终态快照) |
| `reason` | `str` | 非空 |
| `evidence_refs` | `list[str]` | 非空,可程序校验 |
| `valid_from_chapter` | `int` | ≥0,故事内时序 |
| `risk_class` | `str` | 由 `classify_risk` 填 |

**§02/§03/§06/§08 必改项**:四节各自的 `BibleChangeProposal` 定义**全部删除**,改为"`BibleChangeProposal` 权威契约见 §10.3-R8"。具体:§02.2.9 的版本(用 `subject/predicate/old:str/new:str`、缺 `valid_from_chapter/risk_class`)删;§03.1 的版本(`target_id:int`、缺 `risk_class`、缺 `entity`)删;§06 的版本(只有 `op/target_id/old/new/reason/evidence_refs`)删;§08.2.2 的版本(用 `entity_canonical_name/field_path`、`risk_class` 含 `knowledge_edge` 旧拼写)删。

**(b) 为何这样裁定**

四节四套字段,互不兼容:§02 用 `subject/predicate/object`+`old:str/new:str`;§03 用 `target_id:int/entity_id:int`+`valid_from_chapter`(无 risk_class);§06 最简(无 valid_from_chapter/risk_class);§08 用 `entity_canonical_name/field_path`+`risk_class`。LLM 只被允许产出这**一个**契约(硬原则2),它若四节四形态,instructor 的 `response_model` 无所适从、治理平面无法稳定反序列化。裁定取**并集中真正必要的字段**:`op`(加 `retcon` 以承载 §03.7 改设定语义)、`target_id:str`(R2)、`entity`(归属,§08 的 `entity_canonical_name` 简化)、`fact_type`、`old/new:dict`(结构化优于 §02 的 str)、`reason`、`evidence_refs`、`valid_from_chapter`(§03/§08 有,canon 必须绑生效章,硬原则2)、`risk_class`(§08 有,PromotionPolicy 的 require_human_for 命中靠它,且其取值用 R10 的 `knowledge_edge_change`)。`subject/predicate/object` 这类细粒度三元组下沉到 `new` dict 内,不进顶层契约,保持提案对各 fact_type 通用。

---

#### R9 — `beats.beat_type` 唯一取值域 + `value_shift` 改属性

**(a) 权威定义**

```sql
-- §02.6.1 beats 权威 CHECK + value 极性列
CREATE TABLE IF NOT EXISTS beats (
    id              TEXT PRIMARY KEY,
    chapter         INTEGER NOT NULL,
    seq             INTEGER NOT NULL,
    beat_type       TEXT NOT NULL
        CHECK(beat_type IN ('setup','turn','payoff_beat','tension_point','hook')),  -- 唯一五值
    summary         TEXT NOT NULL,
    value_start     TEXT,                          -- 该 beat 价值起态极性(如 '安全'/'+'),取代旧 value_from
    value_end       TEXT,                          -- 该 beat 价值终态极性(如 '危险'/'-'),取代旧 value_to
    tension_level   INTEGER,                       -- tension_point:1-10
    related_foreshadow_id TEXT,                     -- payoff_beat 关联伏笔(TEXT,R2)
    status          TEXT NOT NULL DEFAULT 'planned'
        CHECK(status IN ('planned','drafted','satisfied','dropped')),
    created_at      TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(chapter, seq),
    FOREIGN KEY(related_foreshadow_id) REFERENCES foreshadow(id)
);
```

**`value_shift` 不是 beat_type**,而是**每个 beat 都有的属性**:`value_start`/`value_end`(价值极性)。任一 beat 的价值转向 = `value_start → value_end`。

Pydantic 侧:

```python
class BeatType(str, Enum):
    setup = "setup"
    turn = "turn"
    payoff_beat = "payoff_beat"
    tension_point = "tension_point"
    hook = "hook"

class Beat(BaseModel):
    beat_type: BeatType
    value_start: Optional[str] = None
    value_end: Optional[str] = None
    tension_level: Optional[int] = Field(default=None, ge=1, le=10)
```

**§02 必改项**:§02.6.1 `beat_type` CHECK 从 `('payoff_beat','hook','value_shift','tension_point','setup','turn')` 去掉 `value_shift`,定为五值;`value_from`/`value_to` 列改名 `value_start`/`value_end`。

**§05 必改项**:§05 中 `rising` / `tension` / `payoff` 等 beat 命名**重命名对齐**:`rising`/`tension` → `tension_point`,`payoff` → `payoff_beat`,价值转向用 beat 属性 `value_start/value_end` 表达而非独立 beat 类型。

**(b) 为何这样裁定**

§02.6.1 把 `value_shift` 列进 `beat_type` CHECK,同时又有 `value_from/value_to` 列——逻辑自指:既然每个 beat 都能有 value_from→value_to,那"value_shift 类型的 beat"和"带 value 变化的 setup beat"无法区分,且 §05 还另用 `rising/tension/payoff` 一套命名。McKee 的叙事理论里 value shift(价值转向)是**任何**节拍的可测属性(每个 beat 都在推动价值正负翻转),不是与 setup/turn 并列的一种节拍。故 `beat_type` 收敛为结构性的五类 `{setup, turn, payoff_beat, tension_point, hook}`,value 极性降为列 `value_start/value_end`(改名以避免与旧 `value_from/value_to` 的"故事内时间"语义混淆)。§05 的同义命名对齐到这五值。

---

#### R10 — `require_human_for` 知情者图项统一拼写 `knowledge_edge_change`

**(a) 权威定义**

`config.canon_governance.require_human_for` 中,知情者图变更项的拼写**唯一** = `knowledge_edge_change`。`risk_class`/`fact_type` 体系中表达"知情者图变更"也用此拼写。

```yaml
# config.yaml 权威拼写(§08.3)
canon_governance:
  require_human_for:
    - world_rule
    - power_system
    - character_death
    - foreshadow_payoff
    - knowledge_edge_change      # 知情者图变更(唯一拼写)
```

```python
require_human_for: list[str] = [
    "world_rule", "power_system", "character_death",
    "foreshadow_payoff", "knowledge_edge_change",
]
```

**§01 必改项**:`knowledge_graph_change` → `knowledge_edge_change`。
**§08 必改项**:§08.3 config.yaml 的 `knowledge_edge` 与 §08.2.2 `risk_class` Literal 里的 `knowledge_edge` → `knowledge_edge_change`;§08.2.9 注释"知情者图变更"对齐。
(§03 已用 `knowledge_edge_change`,保持不变,作为对齐基准。)

**(b) 为何这样裁定**

同一安全清单项三处拼写:§01 = `knowledge_graph_change`、§03 = `knowledge_edge_change`、§08 = `knowledge_edge`。`require_human_for` 是 PromotionPolicy 做"高风险强制人审"的命中键(硬原则5 的安全底座),拼写差一字 = 命中失败 = 知情者图变更在 auto 模式下**绕过人审直进 canon**,是安全级 bug。统一以 §03 的 `knowledge_edge_change` 为准:`edge` 与底层表名 `knowledge_edges`(R11)一致(治理的是"边"的变更,不是泛泛的"图"),`_change` 后缀与同清单的动作语义对齐(虽 `world_rule` 等是名词,但知情者图项强调"变更"行为更准确,且 §03 既成事实最广)。

---

#### R11 — `knowledge_edges` DDL 增两列 + `is_public` 定义

**(a) 权威定义**

```sql
-- §02.5.3 knowledge_edges 权威 DDL(增 public_from_chapter + secrecy_level)
CREATE TABLE IF NOT EXISTS knowledge_edges (
    id               TEXT PRIMARY KEY,
    knower_entity_id TEXT NOT NULL,                -- 知情者 → entities(id)
    secret_key       TEXT NOT NULL,                -- 秘密标识(如 'lin_real_identity')
    secret_fact_id   TEXT,                         -- → facts(id)(秘密内容本身,TEXT R2)
    knowledge_state  TEXT NOT NULL
        CHECK(knowledge_state IN ('knows','suspects','unaware','misinformed')),
    learned_chapter  INTEGER NOT NULL,             -- 故事内获知章
    public_from_chapter INTEGER,                   -- 【新增】该秘密对世界公开的起始章;NULL=尚属秘密
    secrecy_level    TEXT,                         -- 【新增】保密级别(见 CHECK)
    source           TEXT,                         -- 如何得知(对白/亲见/推理)
    fact_id          TEXT,                         -- TEXT R2
    created_at       TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY(knower_entity_id) REFERENCES entities(id),
    UNIQUE(knower_entity_id, secret_key, learned_chapter),
    CHECK(secrecy_level IS NULL OR secrecy_level IN ('public','open_secret','secret','top_secret'))
);
CREATE INDEX IF NOT EXISTS idx_know_knower  ON knowledge_edges(knower_entity_id, secret_key, learned_chapter);
CREATE INDEX IF NOT EXISTS idx_know_secret  ON knowledge_edges(secret_key);
CREATE INDEX IF NOT EXISTS idx_know_public  ON knowledge_edges(secret_key, public_from_chapter);
```

`is_public` 判定(供 §04 validator 调用,纯确定性):

```python
def is_public(secret_key: str, chapter: int, conn) -> bool:
    """某秘密在给定章是否已对世界公开。
    依据:存在该 secret_key 的边,其 public_from_chapter 非空且 <= chapter。"""
    row = conn.execute(
        "SELECT MIN(public_from_chapter) AS pf FROM knowledge_edges "
        "WHERE secret_key = ? AND public_from_chapter IS NOT NULL",
        (secret_key,),
    ).fetchone()
    return row["pf"] is not None and row["pf"] <= chapter
```

判定式(权威表述):
> `is_public(info, chapter)` ⟺ `public_from_chapter IS NOT NULL AND public_from_chapter <= chapter`

`secrecy_level` 取值:`{public, open_secret, secret, top_secret}`(NULL 视为未声明,按 `secret` 兜底)。

**§02 必改项**:§02.5.3 `knowledge_edges` 加 `public_from_chapter INTEGER`、`secrecy_level TEXT` 两列 + CHECK + 索引(如上)。
**§04 关联**:validator(知情者图/信息差)引用的 `public_from_chapter`、`secrecy_level`、`is_public()` 自此真实存在,可据此判"某秘密已公开则角色知情不算缺失边"。

**(b) 为何这样裁定**

§02.5.3 原 `knowledge_edges` 只有 `knowledge_state/learned_chapter`,能表达"谁在第几章知道",但**无法表达"这个秘密整体何时对世界公开"**。而 §04 的信息差 validator 需要判断:角色在第 N 章提及某信息,若该信息此时已**公开**,则不算"缺失边违规"(公开信息人人可知);只有当信息仍是秘密、而某未获知角色却表现得知情时,才报 hard issue。缺 `public_from_chapter` 这列,validator 引用的字段不存在(代码读到 NULL/报错)。补两列:`public_from_chapter`(秘密转公开的故事内时点,NULL=仍秘密)给出公开判定的确定性依据;`secrecy_level` 给出保密分级(影响泄密的风险评估与 require_human_for 命中)。`is_public` 用 `MIN(public_from_chapter)` 聚合是因为同一 secret_key 可能有多条边(多个知情者),只要存在一条公开边即视为已公开。

---

#### R12 — World State/工艺表列名收敛(§04/§05/§06 旧名 → §02 权威列名)

**(a) 权威裁定**

§02 是 SQLite DDL **唯一权威**。凡 §04 validator SQL、§05.6 自有 DDL、§06 召回 SQL 中出现的与 §02 不一致的列名/表结构,**一律以 §02 为准改写**;§05.6 全部 `CREATE TABLE` 删除,改"表结构以 §02.6 为权威"。逐表"旧名 → §02 权威列名"映射如下(覆盖 B1/B2/B3/B4/B6 所有列)。

**B1 — World State `*_log` 列名(§04 validator SQL 旧名 → §02.5.* 权威)**

| 表(§02) | §04/旧名 | §02 权威列名 |
|---|---|---|
| `character_power_log`(§02.5.2) | `entity` | `entity_id` |
| | `chapter` | `change_chapter` |
| | `rank_label` | `rank_id`(→ `power_ranks.id`)**+** `rank_order`(冗余整数,免 join 做全序比较) |
| `knowledge_edges`(§02.5.3) | `knower` | `knower_entity_id` |
| | `info_key`(查询键) | `secret_key`(列名);`is_public` 用 `public_from_chapter`(R11) |
| `item_log`(§02.5.6) | `owner` | `item_entity_id`(物品)/ `from_owner_id` / `to_owner_id`(转移双方) |
| | `item` | `item_entity_id` |
| | `qty` | `quantity_delta`(带正负号增量) |
| | `op` | `change_type`(CHECK `acquire/transfer/consume/destroy/craft/lose`) |
| `timeline_events`(§02.5.4) | `story_time` | `story_time_start` **+** `story_time_end`(区间) |
| | `abs_lo`/`abs_hi` | `story_time_start`/`story_time_end` |
| | (无) | `title`(NOT NULL)、`chapter`(叙事章) |
| `travel_edges`(§02.5.5) | `from_loc` | `from_location_id` |
| | `to_loc` | `to_location_id` |
| | `min_hours` | `travel_cost`(+ `time_unit` 声明基准) |
| | `mode` | `travel_mode` |
| `gimmick_rules`(§02.5.7) | `fsm_json` | `activation_cond`(触发条件)+ `constraint_json`(硬限制);FSM 语义由这两列 + `cooldown_*` 表达,**不设独立 `fsm_json` 列** |
| | `cost_schema` | `cost_json` |
| | `preconditions` | `activation_cond` / `constraint_json` |
| | `cooldown` | `cooldown_chapters`(+ 可选 `cooldown_story_time`) |

**B2 — 工艺四表(§05.6 自有 DDL → §02.6.* 权威,§05.6 DDL 删除改引用)**

| 表(§02.6) | §05.6 旧主键/列 | §02 权威 | §02 需扩列(容纳 §05 真需字段) |
|---|---|---|---|
| `beats`(§02.6.1) | 主键 `beat_id` | 主键 `id` | **新增** `value_axis TEXT`(价值轴,如"认可 vs 轻视",B6)、`arc_id TEXT`(所属弧线)、`purpose TEXT`、`intensity INTEGER(1-5)`、`payoff_kind`/`payoff_target`/`consumes_fshadow`/`payoff_effect`(payoff 兑现绑定)、`hook_type`/`hook_strength`(hook 专用) |
| | 章列 `chapter_no` | `chapter` | value 极性已是 `value_start/value_end`(R9),`value_shift` **不是** beat_type |
| `chapter_cards`(§02.6.2) | 主键 `chapter_no` | 主键 `id`(`chapter` 唯一) | 可选并入 `genre_template`/`pacing_mode`/`aggregate_intensity`/`kchars`/`had_small_payoff`/`had_big_payoff`/`hook_type`/`hook_strength`/`craft_score`(§05 节奏指标) |
| `character_cards`(§02.6.3) | 主键 `id`(=canonical_name) | 主键 `id`(`entity_id` 唯一,→ entities) | 可选并入 `want`/`need`/`flaw`/`contradiction`/`values_redline_json`(动机内核轨道);`voice_profile` 列承载 voice_profile_json |
| `pacing_state`(§02.6.5) | 单行累积态 `buildup`/`recent_high_streak`/… | **保留逐章快照**(§02.6.5 不变) | 累积态拆到新表 `pacing_cursor`(见 B4) |

**B3 — foreshadow 状态机统一(§04/§05/§06 旧名 → §02.6.4 权威)**

| 维度 | §04/§05/§06 旧名 | §02.6.4 权威 |
|---|---|---|
| 状态列 | `status` | `state`(CHECK `planted/reinforced/misled/paid_off/overdue`) |
| 到期列 | `deadline_chapter` / `overdue_chapter` | `due_chapter` |
| 到期判定 | `abandoned` 态 / 独立 `overdue_chapter` | **去 `abandoned`**;`overdue` 由 `state NOT IN ('paid_off') AND due_chapter <= N` 计算置位 |

**B4 — pacing 双表分工(新增 `pacing_cursor`)**

`pacing_state`(§02.6.5)**保留逐章快照**(`chapter` 唯一,`tension_level`/`pace`/`value_shift_net`),构成 `tension_curve`;§05.3 `PacingController` 的**单行累积态**拆到**新表 `pacing_cursor`**(单作品单行)。§05.3 决策算法字段一律对齐 `pacing_cursor`。

```sql
-- §02 需新增:pacing_cursor(单行累积态,PacingController 维护)
CREATE TABLE IF NOT EXISTS pacing_cursor (
    id                        INTEGER PRIMARY KEY CHECK(id = 1),   -- 单作品单行
    chapters_since_big_payoff INTEGER NOT NULL DEFAULT 0,
    kchars_since_small_payoff REAL    NOT NULL DEFAULT 0,
    buildup                   INTEGER NOT NULL DEFAULT 0,           -- 蓄力值
    recent_high_streak        INTEGER NOT NULL DEFAULT 0,
    updated_at                TEXT    NOT NULL DEFAULT (datetime('now'))
);
```

**§02 需新增/扩列汇总(本 R12 钉死,§02 与 `schema.sql` 同步)**

1. `facts` 增 `version INTEGER NOT NULL DEFAULT 0`(B5,乐观锁;§11.7 不得游离 ALTER)。
2. `facts.fact_type` CHECK 增 `'constraint'`(G3/R14,全局禁忌落 facts)。
3. `beats` 增 `value_axis TEXT`(B6)与 `arc_id TEXT`(B2);其余 §05 真需列按上表扩。
4. 各 World State `*_log`(`character_power_log` / `knowledge_edges` / `item_log` / `numeric_facts` / `timeline_events`)增 `source_fact_id TEXT REFERENCES facts(id)`(A4,retcon 级联反做/重投影依据)。
5. 新表 `pacing_cursor`(B4)。

**(b) 为何这样裁定**

§02 是 DDL 权威节(SECTION_MAP),validator/算法/召回若各自用不存在的列,建库即崩或读到 NULL 静默失效。一次性以 §02 列名钉死,据此批量改写 §04/§05/§06 全部 SQL 与模型描述,是消除 schema 漂移的最低成本路径。逐章快照(可视化曲线)与单行累积态(决策游标)语义不同、写频不同,拆 `pacing_state`/`pacing_cursor` 两表各司其职。`source_fact_id` 让 `*_log` 行可溯源到 canon fact,retcon 后才能按 entity 全量重放重建投影(见 §16 `reproject_affected`)。

---

#### R13 — 治理装配裁定(PromotionPolicy 唯一签名 + StateTransition 封装 + 提案直出 + 异步候选回调)

**(a) 权威裁定**

1. **`PromotionPolicy.decide` 唯一签名 = 纯函数内核**:
   ```python
   def decide(cand: FactCandidate, world: WorldState, config: CanonGovernanceConfig) -> Route
   ```
   纯确定性、无副作用、无 LLM、可单测(给定 `(cand, world, config)` 必产同一 `Route`)。**staging 写、`promotion_log` 落账、`review_queue` 入队等副作用一律移出 `decide`**,由其调用方(`apply_gate_routes`,见 §16)执行。§07.7.2 的批量版**改名 `decide_batch()` / `GatePlanner`**:显式循环对每个候选调内核 `decide()`,不再叫 `decide`,不再带 `proposals=/state_changes=/staging 写` 形态。(收敛 C1。)

2. **统一 `fact_type`,废 `target_type`**:`PromotionPolicy._is_high_risk` 等一律读 `cand.fact_type`(对齐 §03/§11/§10 R8),**删除 `ch.target_type` 读法**(§07.7.2 line506 `ch.target_type` 改 `cand.fact_type`)。(收敛 C3。)

3. **StateTransition 在 Gate 前规范封装为 `fact_candidate`**:草稿声明的状态迁移(power/knowledge/item/numeric/timeline)在进 Gate 前统一封装成候选(`op='update'`,补 `evidence_refs`/`risk_tier`/`conflict_flags`),走**同一治理路**(同一 `decide` 内核、同一 `promotion_log`)。**facet → fact_type 映射(唯一)**:

   | StateTransition.facet | 封装后 `fact_candidate.fact_type` |
   |---|---|
   | `power` | `power_system` |
   | `knowledge` | `knowledge` |
   | `item` | `item` |
   | `numeric` | `numeric` |
   | `timeline` | `event` |

   (收敛 A2/C3;impl/00 §B.3 补此转换函数 `state_transition_to_candidate()`。)

4. **`BibleChangeProposal` 由 Draft 直出,废独立 ExtractSkill**:canon 级提案由 `ChapterDraftSkill` 在 Draft 阶段**直出**(以 §07/§12 为准),**不设独立 `ExtractSkill`**;§01.4/§01.5 时序图删除"独立 Extract 阶段"。抽取(extract)由 §06 `PipelineManager` 内部承担(非 `SkillTrigger` 位点),§01 措辞"ExtractSkill"改为"§06 抽取管线",§07 不新增该 Skill。(收敛 C4/C5。)

5. **异步 L1/L3 候选回调 `PromotionPolicy.decide` 的入口约定**:`PipelineManager` worker 完成抽取(L1/L3)产出候选后,**回调治理入口**对每个候选调内核 `decide(cand, world, config)` 并执行路由副作用,落 `promotion_log`。入口必带:`actor='system:pipeline'`、`policy_mode`(取自 config)、`run_id`(pipeline 运行 id)、`chapter`(来源章)。在 §07/§06 接缝写明:异步候选**不**永停 `proposed`,而是经此回调走同一 Gate。(收敛 C2。)

**(b) 为何这样裁定**

"唯一决策点"必须真的唯一:纯函数内核可单测、可被同步 Draft 路与异步 pipeline 路复用;副作用外置避免"纯函数里偷偷写库"。`target_type` 两类产物都没有,读它必得 None → require_human_for 漏判 → auto 误晋升高风险(破 HP5/8),故统一 `fact_type`。StateTransition 封装成候选后才有 `candidate_id`/`fact_type`/`risk` 走 `promotion_log`(其 `candidate_id NOT NULL`),也让"状态迁移"和"fact 提案"共用一套治理与审计。提案 Draft 直出消除"生产者/定型时点互斥",使 Check 能校验 proposal。异步候选回调入口堵上"治理真空"。

---

#### R14 — 全局禁忌(taboo)统一(facts + `fact_type='constraint'` + `injection_mode='always'`)

**(a) 权威裁定**

always-on 全局禁忌/否定型约束(如"20 章前不暴露反派身份")的**唯一真相源 = `facts`**,形态固定为:

- `fact_type = 'constraint'`(**§02.2.1 facts CHECK 新增此合法值**,`schema.sql` 同步);
- `injection_mode = 'always'`(§03.8 注入策略,始终硬注入 system 稳定前缀,不走检索);
- 生效区间用 `valid_from_chapter` / `valid_to_chapter`(as-of 过滤,见 R15 与 G5)。

**`config` 降级为 seed**:`config.yaml` 里的禁忌**仅作冷启动 seed 导入**(`seed → BibleChangeProposal(fact_type='constraint')` 经治理落 facts),**不再是运行期真相源**。**删除 `constraints` repo 概念**:§07.5 line264 `ctx.repos.constraints.always_on()` 改为 `facts` 仓储的 `always_on` 只读查询(`WHERE status='canon' AND fact_type='constraint' AND injection_mode='always'` + as-of 过滤);impl/00 `RepositoryBundle` 增**只读 `always_on` 取数**(挂在 facts repo 上),**不增 `constraints` repo**。§06.4.4 措辞从"来自 gimmick_rules/constraint 类 fact"改为"来自 `facts(fact_type='constraint', injection_mode='always')`"。

```sql
-- §02.2.1 facts.fact_type CHECK(R14:增 'constraint'),schema.sql 同步
CHECK(fact_type IN (
    'world_rule','power_system','character_trait','relationship',
    'knowledge','event','item','location','numeric','style','misc',
    'constraint'))                       -- 【新增】全局禁忌/否定型约束
```

```python
# facts 仓储只读 always_on 取数(取代 ctx.repos.constraints.always_on())
def always_on(self, as_of_chapter: int) -> list[Fact]:
    return self.query(
        status="canon", fact_type="constraint", injection_mode="always",
        valid_from_chapter__lte=as_of_chapter,            # R15/G5:as-of 过滤
        valid_to_open_or_after=as_of_chapter,             # valid_to IS NULL OR > N
    )
```

**(b) 为何这样裁定**

always-on 禁忌此前三处互斥(gimmick_rules/`constraint` fact、`injection_mode='always'`、config 硬编码),且 `fact_type='constraint'` 是写不进库的幻影值(§02 CHECK 无)。统一到 `facts` 才能享受 canon 的 append-only 审计、治理闸门、as-of 投影与 1h prompt cache 稳定前缀。config 作 seed 而非运行期真相,避免"改 config 即改运行期禁忌"绕过治理。删 `constraints` repo 是因为它指向不存在的表(G4)。

---

#### R15 — claim 抽取裁定(`extract_claims_rule`(MVP0 确定性)+ `extract_claims_llm`(MVP1+))

**(a) 权威裁定**

`extract_claims` 拆为**两个实现**,由 MVP 阶段 / config 选择,**不再称"一律 LLM"**:

| 实现 | 阶段 | 机制 | 复用 |
|---|---|---|---|
| `extract_claims_rule` | **MVP0**(零 LLM) | 确定性:正则 + 外部词表扫锚点 → 归一到 claim | 复用 §06.3.3 高风险锚点扫描(境界词表/数值正则/地名词表/时间正则) |
| `extract_claims_llm` | **MVP1+** | LLM(Haiku/Sonnet)+ instructor/Pydantic 自愈重试 | §04.1 既有抽取 |

`extract_claims_rule` 是 **MVP0 五个确定性 validator 的前置依赖**(validator 吃 claim,无 claim 则 Check 空转);§04.1 措辞改"由 MVP 阶段/config 选 rule 版或 llm 版",并补确定性版规范(钉为 MVP0 前置)。

```python
def extract_claims(draft, *, mode: str) -> list[Claim]:
    # mode 取自 config.extract.claim_mode(MVP0='rule';MVP1+='llm')
    return extract_claims_rule(draft) if mode == "rule" else extract_claims_llm(draft)
```

**(b) 为何这样裁定**

§04.1 称 LLM 抽取、impl 称确定性,MVP0 又要"零 LLM 抽取" → 三处矛盾且 MVP0 Check 无 claim 来源。确定性正则+词表抽取(复用 §06.3.3 已有的锚点扫描,召回率优先)恰好满足 MVP0"可单测、零 LLM、不漏高风险锚点",LLM 版留到 MVP1+ 做语义消歧/归一。两版同签名、由 config 切换,validator 对 claim 来源无感。

---

### 10.4 落定后全局不变量(对齐自检清单)

1. **ID 类型**:凡指向 fact 的列 = `TEXT`;凡主键 = `TEXT` 前缀化(`fact_`/`cand_`/`rev_`/`plog_`/`ent_`/`rq_`)。(R2/R3/R7)
2. **状态词不重叠**:候选用 `{proposed, pending_review, promoted, rejected, superseded}`;事实用 `{canon, tentative, retconned}`。`promoted≠canon`,`superseded≠retconned`。(R3/R4)
3. **决策四态**:`Route ∈ {COMMIT, REVIEW, HOLD, REJECT}`;`promotion_log.decision` 含此四态 + `revert`。(R5)
4. **检索两表**:facts 检索 = `facts_fts`;草稿检索 = `drafts_fts`;jieba 在应用层,`tokenize=unicode61`;可选 `facts_fts_tri`。(R6)
5. **审计单表**:`promotion_log` 唯一权威 DDL 在 §02;无 `canon_changelog`,内容变更流归 `fact_revisions`。(R7)
6. **提案单契约**:`BibleChangeProposal` 唯一定义在 §10.3-R8(op 含 `retcon`,字段含 `valid_from_chapter`/`risk_class`)。(R8)
7. **beat 五型**:`beat_type ∈ {setup, turn, payoff_beat, tension_point, hook}`;value 极性 = 列 `value_start/value_end`。(R9)
8. **安全键拼写**:`knowledge_edge_change`(唯一)。(R10)
9. **知情者公开字段**:`knowledge_edges.public_from_chapter` + `secrecy_level` 真实存在;`is_public` 判定式固定。(R11)
10. **交叉引用**:全部用 §10.1 SECTION_MAP 的 01-11 实际节号。(R1)
11. **列名权威**:World State/工艺表列名一律以 §02 为准;§05.6 自有 DDL 删除改引用;foreshadow 用 `state`/`due_chapter`(去 `abandoned`);pacing 拆 `pacing_state`(逐章快照)+ `pacing_cursor`(单行累积态);`*_log` 增 `source_fact_id`;`facts` 增 `version`;`beats` 增 `value_axis`/`arc_id`。(R12)
12. **治理唯一签名**:`PromotionPolicy.decide(cand, world, config) -> Route` 为纯函数内核(无副作用);批量 = `decide_batch`/`GatePlanner` 循环调内核;统一 `fact_type`(废 `target_type`);StateTransition 经 facet→fact_type 封装为候选走同一治理路;提案 Draft 直出(废独立 ExtractSkill);异步候选回调 `decide` 入口(actor=`system:pipeline`)。(R13)
13. **全局禁忌**:唯一真相 = `facts` + `fact_type='constraint'`(§02 CHECK 含)+ `injection_mode='always'` + as-of 区间;config 仅作 seed;无 `constraints` repo,统一 facts 仓储 `always_on`。(R14)
14. **claim 抽取双实现**:`extract_claims_rule`(MVP0 确定性,正则+词表,复用 §06.3.3)/ `extract_claims_llm`(MVP1+),由 MVP 阶段/config 选;rule 版是 MVP0 五 validator 前置。(R15)
